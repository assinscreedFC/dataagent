---
phase: 01-state-foundation-minimal-graph
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/dataagent/config.py
  - src/dataagent/agent/__init__.py
  - src/dataagent/agent/state.py
  - src/dataagent/agent/llm.py
  - tests/test_state.py
autonomous: true
requirements: [GRAPH-01, GRAPH-05]
must_haves:
  truths:
    - "AgentState porte exactement les 8 champs du PLAN.md (question, plan, findings, messages, iterations, max_iterations, db, report)"
    - "Le champ db est annoté UntrackedValue donc jamais checkpointé"
    - "findings accumule via reducer add, messages via add_messages"
    - "max_iterations a une valeur par défaut (MAX_ITERATIONS=5) et iterations démarre à 0"
    - "Une factory LLM construit ChatGoogleGenerativeAI avec le bon modèle et la clé GEMINI_API_KEY"
  artifacts:
    - path: "src/dataagent/agent/state.py"
      provides: "AgentState TypedDict + helper initial_state()"
      contains: "class AgentState"
    - path: "src/dataagent/agent/llm.py"
      provides: "Factory ChatGoogleGenerativeAI (planner Flash / synthesizer Pro)"
      contains: "ChatGoogleGenerativeAI"
    - path: "src/dataagent/config.py"
      provides: "Constantes modèles Gemini + MAX_ITERATIONS"
      contains: "GEMINI_MODEL_FLASH"
  key_links:
    - from: "src/dataagent/agent/state.py"
      to: "langgraph.channels.UntrackedValue"
      via: "Annotated[DuckDBPyConnection, UntrackedValue()]"
      pattern: "UntrackedValue"
    - from: "src/dataagent/agent/llm.py"
      to: "config.GEMINI_MODEL_FLASH / GEMINI_MODEL_PRO"
      via: "import depuis config"
      pattern: "GEMINI_MODEL"
---

<objective>
Poser les fondations contractuelles de l'agent : les constantes de config (modèles Gemini + garde-fou coût), le state schema `AgentState` complet (8 champs, conforme PLAN.md §State schema et D-01), et une factory LLM Gemini. Ces artefacts sont les CONTRATS consommés par les nodes (plan 02) et le graphe (plan 03) — aucune exploration de code ne doit être nécessaire en aval.

Purpose: Sans state schema ni factory LLM, aucun node ne peut être écrit. Interface-first.
Output: `config.py` enrichi, `agent/state.py`, `agent/llm.py`, `tests/test_state.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-state-foundation-minimal-graph/01-CONTEXT.md
@PLAN.md
@src/dataagent/config.py
@src/dataagent/data/loader.py
@tests/conftest.py

<interfaces>
<!-- Contrats vérifiés au moment du plan. L'exécuteur doit les utiliser directement. -->

API langchain-google-genai (vérifiée juin 2026) :
```python
from langchain_google_genai import ChatGoogleGenerativeAI
llm = ChatGoogleGenerativeAI(model="gemini-2.0-flash", api_key="<key>")
# fallback automatique : GOOGLE_API_KEY puis GEMINI_API_KEY si api_key absent
resp = llm.invoke([("system", "..."), ("human", "...")])  # resp.content : str
```

API langgraph UntrackedValue (vérifiée juin 2026) :
```python
from langgraph.channels import UntrackedValue   # channel annotation
# usage : champ TypedDict annoté -> jamais checkpointé
db: Annotated[DuckDBPyConnection, UntrackedValue()]
```
ATTENTION : PLAN.md écrit `UntrackedValue[DuckDBConn]` (subscription) — c'est FAUX.
La syntaxe correcte est `Annotated[T, UntrackedValue()]` (annotation dans Annotated).

Reducers :
```python
from operator import add
from langgraph.graph.message import add_messages
findings: Annotated[list[dict], add]
messages: Annotated[list, add_messages]
```

Connexion DuckDB existante (loader J1) :
```python
from dataagent.data.loader import connect, load_csvs_to_duckdb
conn = connect()                         # DuckDBPyConnection (":memory:")
tables = load_csvs_to_duckdb(conn, csv_dir)  # list[str] noms de tables
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ajouter les constantes Gemini + garde-fou à config.py</name>
  <files>src/dataagent/config.py</files>
  <read_first>
    - src/dataagent/config.py (fichier modifié — voir constantes existantes MAX_ITERATIONS=5, DEFAULT_TOP_N)
    - .planning/phases/01-state-foundation-minimal-graph/01-CONTEXT.md (D-07, D-10)
  </read_first>
  <action>
    Dans `src/dataagent/config.py`, ajouter sous les constantes existantes (NE PAS toucher PROJECT_ROOT, DATA_RAW, REPORTS, DEFAULT_TOP_N, MAX_ITERATIONS) :
    - `import os` en tête.
    - `GEMINI_MODEL_FLASH = os.environ.get("GEMINI_MODEL_FLASH", "gemini-2.0-flash")` — planner + génération SQL (D-07).
    - `GEMINI_MODEL_PRO = os.environ.get("GEMINI_MODEL_PRO", "gemini-2.5-pro")` — synthesizer (D-07).
    - `GEMINI_API_KEY = os.environ.get("GEMINI_API_KEY")` — clé chargée depuis l'env (le `.env` est lu via python-dotenv dans le CLI, plan 03).
    `MAX_ITERATIONS = 5` existe déjà : le laisser tel quel (D-10). Ajouter un commentaire court "# garde-fou coût boucle critic (J4)".
    Override env possible (D-07) : les noms de modèles passent par os.environ.get avec défaut.
  </action>
  <acceptance_criteria>
    - `grep -q "GEMINI_MODEL_FLASH" src/dataagent/config.py` réussit
    - `grep -q "GEMINI_MODEL_PRO" src/dataagent/config.py` réussit
    - `grep -q "gemini-2.0-flash" src/dataagent/config.py` réussit
    - `grep -q "gemini-2.5-pro" src/dataagent/config.py` réussit
    - `python -c "from dataagent.config import GEMINI_MODEL_FLASH, GEMINI_MODEL_PRO, MAX_ITERATIONS; print(MAX_ITERATIONS)"` affiche `5`
  </acceptance_criteria>
  <verify>
    <automated>python -c "from dataagent.config import GEMINI_MODEL_FLASH, GEMINI_MODEL_PRO, MAX_ITERATIONS, GEMINI_API_KEY; assert GEMINI_MODEL_FLASH=='gemini-2.0-flash'; assert GEMINI_MODEL_PRO=='gemini-2.5-pro'; assert MAX_ITERATIONS==5; print('ok')"</automated>
  </verify>
  <done>config.py expose GEMINI_MODEL_FLASH, GEMINI_MODEL_PRO, GEMINI_API_KEY, MAX_ITERATIONS ; constantes existantes intactes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Définir AgentState (state.py) + initial_state()</name>
  <files>src/dataagent/agent/__init__.py, src/dataagent/agent/state.py</files>
  <read_first>
    - PLAN.md (§State schema — les 8 champs exacts)
    - .planning/phases/01-state-foundation-minimal-graph/01-CONTEXT.md (D-01, D-02, D-03, D-10)
    - src/dataagent/config.py (MAX_ITERATIONS)
    - src/dataagent/data/loader.py (type DuckDBPyConnection)
  </read_first>
  <behavior>
    - Test : `AgentState.__annotations__` contient exactement les 8 clés {question, plan, findings, messages, iterations, max_iterations, db, report}
    - Test : `initial_state(question, db)` retourne un dict avec question=question, plan=[], findings=[], messages=[], iterations=0, max_iterations=MAX_ITERATIONS (5), report="", db=db
    - Test : le champ `db` du schema est annoté avec UntrackedValue (vérifier via typing.get_type_hints / get_args que UntrackedValue est dans les metadata de l'annotation)
    - Test : `findings` est annoté avec le reducer `add` (operator.add dans les metadata)
  </behavior>
  <action>
    Créer `src/dataagent/agent/__init__.py` (vide ou docstring 1 ligne) et `src/dataagent/agent/state.py`.
    Dans state.py :
    ```python
    from operator import add
    from typing import Annotated, TypedDict

    from duckdb import DuckDBPyConnection
    from langgraph.channels import UntrackedValue
    from langgraph.graph.message import add_messages

    from dataagent.config import MAX_ITERATIONS


    class AgentState(TypedDict):
        question: str
        plan: list[str]
        findings: Annotated[list[dict], add]
        messages: Annotated[list, add_messages]
        iterations: int
        max_iterations: int
        db: Annotated[DuckDBPyConnection, UntrackedValue()]
        report: str
    ```
    Conforme D-01 EXACTEMENT (8 champs, ni plus ni moins). `db` annoté UntrackedValue (D-02, success criterion #4). `findings` reducer `add`, `messages` add_messages (D-03).
    Ajouter un helper `initial_state(question: str, db: DuckDBPyConnection) -> AgentState` qui retourne le dict initial : plan=[], findings=[], messages=[], iterations=0, max_iterations=MAX_ITERATIONS, report="", db=db, question=question (D-10 : iterations init 0, max_iterations câblé).
    Type hints sur toutes les signatures (règle projet). Pas de print, pas de secret.
  </action>
  <acceptance_criteria>
    - `grep -q "class AgentState" src/dataagent/agent/state.py` réussit
    - `grep -q "UntrackedValue" src/dataagent/agent/state.py` réussit
    - `grep -q "add_messages" src/dataagent/agent/state.py` réussit
    - `grep -q "def initial_state" src/dataagent/agent/state.py` réussit
    - `python -c "from dataagent.agent.state import AgentState; assert set(AgentState.__annotations__)=={'question','plan','findings','messages','iterations','max_iterations','db','report'}; print('ok')"` affiche `ok`
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_state.py -q</automated>
  </verify>
  <done>AgentState a exactement 8 champs, db en UntrackedValue, findings reducer add, messages add_messages ; initial_state() pose iterations=0 et max_iterations=5 ; tests passent.</done>
</task>

<task type="auto">
  <name>Task 3: Factory LLM Gemini (llm.py) + tests state</name>
  <files>src/dataagent/agent/llm.py, tests/test_state.py</files>
  <read_first>
    - src/dataagent/config.py (GEMINI_MODEL_FLASH, GEMINI_MODEL_PRO, GEMINI_API_KEY)
    - src/dataagent/agent/state.py (créé task 2)
    - tests/conftest.py (pattern fixtures, fixture conn)
    - .planning/phases/01-state-foundation-minimal-graph/01-CONTEXT.md (D-06, D-07)
  </read_first>
  <action>
    Créer `src/dataagent/agent/llm.py` avec deux factory functions (D-06, D-07) :
    ```python
    from langchain_google_genai import ChatGoogleGenerativeAI
    from dataagent.config import GEMINI_API_KEY, GEMINI_MODEL_FLASH, GEMINI_MODEL_PRO

    def flash_llm() -> ChatGoogleGenerativeAI:
        """LLM rapide/cheap : planner + génération SQL."""
        return ChatGoogleGenerativeAI(model=GEMINI_MODEL_FLASH, api_key=GEMINI_API_KEY, temperature=0)

    def pro_llm() -> ChatGoogleGenerativeAI:
        """LLM qualité : synthesizer du rapport."""
        return ChatGoogleGenerativeAI(model=GEMINI_MODEL_PRO, api_key=GEMINI_API_KEY, temperature=0)
    ```
    `temperature=0` pour déterminisme (utile à l'eval J4). NE PAS hardcoder les noms de modèles (importer de config — D-07). NE PAS logger la clé.

    Créer `tests/test_state.py` (vraie I/O DuckDB via fixture `conn` de conftest.py, AUCUN mock I/O — règle projet) :
    - `test_agent_state_has_exactly_8_fields` : set(AgentState.__annotations__) == les 8 clés.
    - `test_db_field_is_untracked` : via `typing.get_type_hints(AgentState, include_extras=True)` + `typing.get_args`, vérifier qu'une instance de `UntrackedValue` figure dans les metadata du champ `db`.
    - `test_findings_uses_add_reducer` : `operator.add` figure dans les metadata du champ `findings`.
    - `test_initial_state_defaults` (utilise fixture `conn`) : initial_state("Q?", conn) -> iterations==0, max_iterations==5, plan==[], findings==[], report=="", db is conn, question=="Q?".
    Ne PAS tester d'appel réseau LLM ici (les factory ne sont pas instanciées en test — l'instanciation réseau est couverte par le run e2e plan 03 ou un test déterministe).
  </action>
  <acceptance_criteria>
    - `grep -q "def flash_llm" src/dataagent/agent/llm.py` réussit
    - `grep -q "def pro_llm" src/dataagent/agent/llm.py` réussit
    - `grep -q "GEMINI_MODEL_FLASH" src/dataagent/agent/llm.py` réussit (pas de modèle hardcodé)
    - `python -m pytest tests/test_state.py -q` sort en code 0
    - `python -c "import dataagent.agent.llm"` n'erreur pas (import seul, sans réseau)
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_state.py -q && python -c "import dataagent.agent.llm; print('import ok')"</automated>
  </verify>
  <done>llm.py expose flash_llm()/pro_llm() pointant sur les constantes config ; tests state passent (8 champs, db untracked, findings add, initial_state defaults).</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_state.py -q` passe (au moins 4 tests).
- Import sans réseau OK : `python -c "from dataagent.agent.state import AgentState, initial_state; import dataagent.agent.llm"`.
- Aucun nom de modèle hardcodé dans llm.py (grep GEMINI_MODEL_FLASH présent).
- Le champ `db` est bien annoté UntrackedValue (test dédié).
</verification>

<success_criteria>
- AgentState conforme D-01 (8 champs exacts), db UntrackedValue (criterion #4), findings reducer add, messages add_messages.
- max_iterations câblé avec défaut 5, iterations=0 dans initial_state (criterion #3, GRAPH-05).
- Factory LLM Gemini prête (Flash + Pro), modèles depuis config (GRAPH-03/04 préparés).
- Suite de tests verte, couverture du nouveau module state ≥80%.
</success_criteria>

<output>
After completion, create `.planning/phases/01-state-foundation-minimal-graph/01-01-SUMMARY.md`
</output>
