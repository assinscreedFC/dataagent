---
phase: 01-state-foundation-minimal-graph
plan: 02
type: execute
wave: 2
depends_on: [1]
files_modified:
  - src/dataagent/agent/schema_introspect.py
  - src/dataagent/agent/nodes.py
  - tests/test_nodes.py
autonomous: true
requirements: [GRAPH-03, GRAPH-04]
must_haves:
  truths:
    - "Le node planner (Gemini Flash) décompose la question en sous-questions plan[]"
    - "Le sql_tool minimal génère du SQL depuis la sous-question + le schema DuckDB, l'exécute, pousse un finding"
    - "Un finding contient la query SQL, les tables touchées et les données (sourçabilité)"
    - "Le node synthesizer (Gemini Pro) produit un rapport markdown qui cite ses sources"
    - "Sur erreur SQL, le sql_tool remonte l'erreur proprement dans le finding (pas de retry, pas de crash)"
  artifacts:
    - path: "src/dataagent/agent/nodes.py"
      provides: "planner_node, sql_tool_node, synthesizer_node"
      contains: "def planner_node"
    - path: "src/dataagent/agent/schema_introspect.py"
      provides: "Introspection du schema DuckDB (tables + colonnes) pour le prompt SQL"
      contains: "def schema_description"
  key_links:
    - from: "src/dataagent/agent/nodes.py"
      to: "state['db'] (DuckDB connection)"
      via: "conn.execute(sql).fetchall()"
      pattern: "state\\[.db.\\]"
    - from: "src/dataagent/agent/nodes.py"
      to: "findings (reducer add)"
      via: "return {'findings': [finding]}"
      pattern: "findings"
    - from: "src/dataagent/agent/nodes.py"
      to: "llm.flash_llm / pro_llm"
      via: "import depuis agent.llm"
      pattern: "flash_llm|pro_llm"
---

<objective>
Implémenter les trois nodes de la boucle minimale, contre le contrat AgentState du plan 01 :
- `planner_node` (Flash) : question -> `plan[]` (GRAPH-03, D-08, criterion #5).
- `sql_tool_node` (Flash pour la génération SQL) : génère SQL depuis le schema DuckDB introspecté, exécute, pousse un finding sourcé (D-12). Minimal : pas de validation/retry (phase 2). Erreur SQL -> finding d'erreur propre, pas de crash.
- `synthesizer_node` (Pro) : findings -> rapport markdown citant ses sources (GRAPH-04, D-09, criterion #2).

Purpose: Ce sont les unités de travail consommées par le graphe (plan 03). Définir aussi l'introspection schema pour donner au LLM les vrais noms de tables/colonnes (anti-hallucination de base).
Output: `agent/schema_introspect.py`, `agent/nodes.py`, `tests/test_nodes.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-state-foundation-minimal-graph/01-CONTEXT.md
@PLAN.md
@src/dataagent/agent/state.py
@src/dataagent/agent/llm.py
@src/dataagent/data/queries.py
@src/dataagent/data/loader.py
@tests/conftest.py

<interfaces>
<!-- Contrats du plan 01 (déjà implémentés). Utiliser directement. -->

State + helper (agent/state.py) :
```python
class AgentState(TypedDict):
    question: str
    plan: list[str]
    findings: Annotated[list[dict], add]
    messages: Annotated[list, add_messages]
    iterations: int
    max_iterations: int
    db: Annotated[DuckDBPyConnection, UntrackedValue()]
    report: str

def initial_state(question: str, db: DuckDBPyConnection) -> AgentState: ...
```

Factory LLM (agent/llm.py) :
```python
def flash_llm() -> ChatGoogleGenerativeAI: ...   # planner + SQL gen
def pro_llm() -> ChatGoogleGenerativeAI: ...     # synthesizer
# usage : flash_llm().invoke([("system", "..."), ("human", "...")]).content -> str
```

Schema DuckDB introspectable (tables Olist chargées) :
```python
# liste des tables : SELECT table_name FROM information_schema.tables
# colonnes : SELECT column_name, data_type FROM information_schema.columns WHERE table_name = ?
```

Convention node LangGraph : un node = fonction `(state: AgentState) -> dict` qui retourne
UNIQUEMENT les clés du state à mettre à jour (les reducers fusionnent). Ex : `return {"plan": [...]}`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Introspection schema DuckDB (schema_introspect.py)</name>
  <files>src/dataagent/agent/schema_introspect.py</files>
  <read_first>
    - src/dataagent/data/loader.py (table_name, load_csvs_to_duckdb)
    - src/dataagent/data/queries.py (schéma réel : orders, order_items, products, order_reviews...)
    - tests/conftest.py (fixture conn — mini Olist)
  </read_first>
  <behavior>
    - Test (fixture conn) : `schema_description(conn)` retourne une str non vide listant les tables du mini Olist (orders, order_items, products, order_reviews)
    - Test : pour la table `orders`, la description contient au moins une de ses colonnes (ex: order_id ou order_status)
    - Test : le format est exploitable dans un prompt (chaque table avec ses colonnes), déterministe (même conn -> même sortie)
  </behavior>
  <action>
    Créer `src/dataagent/agent/schema_introspect.py` avec :
    ```python
    def schema_description(conn) -> str:
        """Texte décrivant tables + colonnes DuckDB, pour injection dans le prompt SQL."""
    ```
    Implémentation : `SELECT table_name FROM information_schema.tables WHERE table_schema='main' ORDER BY table_name`, puis pour chaque table `SELECT column_name, data_type FROM information_schema.columns WHERE table_name=? ORDER BY ordinal_position`. Construire une str du type :
    `TABLE orders(order_id VARCHAR, customer_id VARCHAR, order_status VARCHAR, ...)` une ligne par table.
    Requêtes paramétrées uniquement (pas de f-string sur input — règle sécurité). Type hint sur la signature.
    Cette str alimente le prompt du sql_tool pour donner les VRAIS noms (anti-hallucination de base, D-12).
  </action>
  <acceptance_criteria>
    - `grep -q "def schema_description" src/dataagent/agent/schema_introspect.py` réussit
    - `grep -q "information_schema" src/dataagent/agent/schema_introspect.py` réussit
    - `python -m pytest tests/test_nodes.py -q -k schema` sort en code 0
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_nodes.py -q -k schema</automated>
  </verify>
  <done>schema_description(conn) liste tables + colonnes Olist sous forme texte déterministe, requêtes paramétrées.</done>
</task>

<task type="auto">
  <name>Task 2: planner_node + sql_tool_node (nodes.py)</name>
  <files>src/dataagent/agent/nodes.py</files>
  <read_first>
    - src/dataagent/agent/state.py (AgentState, convention node)
    - src/dataagent/agent/llm.py (flash_llm)
    - src/dataagent/agent/schema_introspect.py (schema_description — task 1)
    - .planning/phases/01-state-foundation-minimal-graph/01-CONTEXT.md (D-08, D-12)
  </read_first>
  <action>
    Créer `src/dataagent/agent/nodes.py`.

    `planner_node(state: AgentState) -> dict` (GRAPH-03, D-08) :
    - Construit un prompt Flash : "Décompose cette question business en 1 à 4 sous-questions analytiques précises, une par ligne, sans numérotation." + state["question"].
    - Appelle `flash_llm().invoke([...]).content`, parse les lignes non vides en `list[str]`.
    - Si le LLM renvoie une seule ligne, plan = [cette ligne] (au minimum la question elle-même). Garantir plan non vide.
    - Retourne `{"plan": plan}` (le plan[] est visible dans le state — criterion #5).

    `sql_tool_node(state: AgentState) -> dict` (D-12, scope minimal) :
    - Récupère `conn = state["db"]`, `schema = schema_description(conn)`.
    - Prend la/les sous-question(s) du plan (Phase 1 : traiter la première sous-question OU itérer sur plan et produire un finding par sous-question — au choix, documenter). 
    - Prompt Flash : "Voici le schema DuckDB :\n{schema}\nÉcris UNE requête SQL DuckDB valide qui répond à : {sous_question}. Réponds avec le SQL brut uniquement, sans markdown." Nettoyer un éventuel fence ```sql.
    - Exécute `conn.execute(sql).fetchall()` (+ `.columns` via description) dans un try/except.
      - Succès : finding = `{"source": "sql_tool", "subquestion": sq, "sql": sql, "tables": <tables extraites du schema mentionnées dans le sql>, "rows": rows, "columns": cols}`.
      - Erreur (catch `duckdb.Error` / Exception large en dernier recours, logger avec contexte) : finding = `{"source": "sql_tool", "subquestion": sq, "sql": sql, "error": str(e)}`. PAS de retry (phase 2), pas de re-raise — on remonte proprement (D-12).
    - Retourne `{"findings": [finding, ...], "iterations": state["iterations"] + 1}` (incrémente iterations — structure prête phase 4, D-10).
    Type hints, docstrings courtes, pas de print (logging), pas de secret loggé.
  </action>
  <acceptance_criteria>
    - `grep -q "def planner_node" src/dataagent/agent/nodes.py` réussit
    - `grep -q "def sql_tool_node" src/dataagent/agent/nodes.py` réussit
    - `grep -q "findings" src/dataagent/agent/nodes.py` réussit
    - `grep -q "iterations" src/dataagent/agent/nodes.py` réussit (incrément câblé)
    - `python -c "from dataagent.agent.nodes import planner_node, sql_tool_node; print('ok')"` affiche `ok`
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_nodes.py -q -k "sql_tool or planner"</automated>
  </verify>
  <done>planner_node retourne plan[] non vide ; sql_tool_node génère+exécute SQL, pousse un finding sourcé (sql+tables+rows) ou un finding d'erreur propre, incrémente iterations ; pas de retry.</done>
</task>

<task type="auto">
  <name>Task 3: synthesizer_node + tests nodes</name>
  <files>src/dataagent/agent/nodes.py, tests/test_nodes.py</files>
  <read_first>
    - src/dataagent/agent/state.py (AgentState)
    - src/dataagent/agent/llm.py (pro_llm)
    - src/dataagent/agent/nodes.py (format des findings — task 2)
    - tests/conftest.py (fixture conn pour I/O réelle)
    - .planning/phases/01-state-foundation-minimal-graph/01-CONTEXT.md (D-09)
  </read_first>
  <action>
    Dans `src/dataagent/agent/nodes.py`, ajouter `synthesizer_node(state: AgentState) -> dict` (GRAPH-04, D-09) :
    - Sérialise les findings (sql, tables, rows, ou error) en texte.
    - Prompt Pro : "Tu es analyste data. À partir de ces findings, rédige un rapport markdown concis qui répond à la question. CITE explicitement tes sources : pour chaque chiffre, indique la/les table(s) et la query SQL utilisées. Question : {question}\nFindings :\n{findings_text}".
    - Appelle `pro_llm().invoke([...]).content`, retourne `{"report": report}`.
    - Le rapport DOIT être markdown et citer tables/queries (criterion #2). Si findings vide ou tous en erreur, produire un rapport markdown expliquant l'échec proprement.

    Créer `tests/test_nodes.py`. Stratégie de test (règle projet : vraie I/O DuckDB, mock UNIQUEMENT le LLM) :
    - Tests `schema_description` (task 1) avec fixture `conn`.
    - `test_planner_node_returns_nonempty_plan` : monkeypatch `dataagent.agent.nodes.flash_llm` pour renvoyer un fake LLM dont `.invoke().content` = "CA 2017 ?\nNombre de commandes ?". Vérifier `planner_node(state)["plan"] == ["CA 2017 ?", "Nombre de commandes ?"]`.
    - `test_sql_tool_executes_and_pushes_finding` : fake flash_llm renvoyant un SQL valide sur le mini Olist (ex: `SELECT COUNT(*) AS n FROM orders`). state["db"] = fixture conn. Vérifier le finding contient `sql`, `rows` (vraie exécution DuckDB), et que `iterations` est incrémenté.
    - `test_sql_tool_handles_sql_error` : fake flash_llm renvoyant un SQL invalide (`SELECT * FROM table_inexistante`). Vérifier finding contient une clé `error`, AUCUNE exception propagée, et le node retourne quand même un dict.
    - `test_synthesizer_produces_markdown_with_sources` : fake pro_llm renvoyant un markdown contenant "orders". state["findings"] = [un finding avec sql+tables]. Vérifier `synthesizer_node(state)["report"]` non vide et de type str.
    Utiliser une petite classe fake LLM (méthode `invoke` -> objet avec attribut `.content`) — c'est le SEUL mock autorisé (l'appel réseau LLM), conforme code_context.
    Viser couverture ≥80% sur nodes.py et schema_introspect.py.
  </action>
  <acceptance_criteria>
    - `grep -q "def synthesizer_node" src/dataagent/agent/nodes.py` réussit
    - `grep -q "markdown" src/dataagent/agent/nodes.py` (instruction de sourçage dans le prompt) réussit
    - `python -m pytest tests/test_nodes.py -q` sort en code 0
    - `python -m pytest tests/test_nodes.py --cov=src/dataagent/agent/nodes --cov=src/dataagent/agent/schema_introspect --cov-report=term-missing -q` montre ≥80% sur ces 2 modules
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_nodes.py -q</automated>
  </verify>
  <done>synthesizer_node produit un rapport markdown sourcé ; tests des 3 nodes verts (LLM mocké, DuckDB réel) ; couverture ≥80% sur nodes.py et schema_introspect.py.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_nodes.py -q` passe.
- Les 3 nodes importables : `python -c "from dataagent.agent.nodes import planner_node, sql_tool_node, synthesizer_node"`.
- sql_tool gère l'erreur SQL sans crasher (test dédié) — conforme D-12 "remonter proprement, pas de retry".
- findings sourcés (sql + tables) ; report markdown citant les sources.
</verification>

<success_criteria>
- planner décompose en plan[] non vide (GRAPH-03, criterion #5).
- sql_tool génère+exécute SQL réel sur DuckDB, finding sourcé, iterations incrémenté, erreur propre (D-12).
- synthesizer produit markdown sourcé (GRAPH-04, criterion #2).
- Tests verts, couverture ≥80% sur les nouveaux modules, LLM mocké / DuckDB réel.
</success_criteria>

<output>
After completion, create `.planning/phases/01-state-foundation-minimal-graph/01-02-SUMMARY.md`
</output>
