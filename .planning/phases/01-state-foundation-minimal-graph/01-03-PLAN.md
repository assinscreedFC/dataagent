---
phase: 01-state-foundation-minimal-graph
plan: 03
type: execute
wave: 3
depends_on: [1, 2]
files_modified:
  - src/dataagent/agent/graph.py
  - src/dataagent/__main__.py
  - tests/test_graph.py
autonomous: true
requirements: [GRAPH-02, GRAPH-06]
user_setup:
  - service: gemini
    why: "Appels LLM planner (Flash) et synthesizer (Pro) pour le run end-to-end"
    env_vars:
      - name: GEMINI_API_KEY
        source: ".env à la racine (déjà présent) — Google AI Studio -> Get API key"
must_haves:
  truths:
    - "Un StateGraph compilé câble START -> planner -> sql_tool -> synthesizer -> END"
    - "python -m dataagent \"CA total 2017 ?\" build le graph, l'invoke et print le rapport"
    - "Le run produit une réponse correcte sourcée pour une question simple"
    - "max_iterations est observable dans le state final du run (hard stop câblé)"
    - "La connexion DuckDB est injectée dans le state initial via UntrackedValue (jamais checkpointée)"
  artifacts:
    - path: "src/dataagent/agent/graph.py"
      provides: "build_graph() -> CompiledStateGraph + run(question) helper"
      contains: "StateGraph"
    - path: "src/dataagent/__main__.py"
      provides: "Entrypoint CLI python -m dataagent \"question\""
      contains: "load_dotenv"
  key_links:
    - from: "src/dataagent/agent/graph.py"
      to: "nodes (planner/sql_tool/synthesizer)"
      via: "add_node + add_edge linéaires"
      pattern: "add_edge"
    - from: "src/dataagent/__main__.py"
      to: "agent.graph.run + loader (DuckDB)"
      via: "build conn -> initial_state -> invoke -> print report"
      pattern: "invoke|run"
    - from: "src/dataagent/__main__.py"
      to: "GEMINI_API_KEY"
      via: "load_dotenv() avant build"
      pattern: "load_dotenv"
---

<objective>
Câbler le graphe linéaire minimal et l'entrypoint CLI, contre les nodes du plan 02 et le state du plan 01 :
- `build_graph()` : `StateGraph(AgentState)` -> add 3 nodes -> edges `START → planner → sql_tool → synthesizer → END` -> `.compile()` (D-04, GRAPH-02).
- Création de la connexion DuckDB à l'entrée (loader J1) injectée dans le state initial en UntrackedValue (D-05, criterion #4).
- `python -m dataagent "question"` : load_dotenv, build conn + graph, invoke, print report (D-11, GRAPH-06, criterion #1).

Purpose: C'est le câblage final qui rend l'agent invocable end-to-end. Wire-only contre des contrats déjà testés.
Output: `agent/graph.py`, `__main__.py`, `tests/test_graph.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-state-foundation-minimal-graph/01-CONTEXT.md
@PLAN.md
@src/dataagent/agent/state.py
@src/dataagent/agent/nodes.py
@src/dataagent/config.py
@src/dataagent/data/loader.py
@tests/conftest.py

<interfaces>
<!-- Contrats des plans 01 et 02 (déjà implémentés). Utiliser directement. -->

State (agent/state.py) :
```python
class AgentState(TypedDict): ...   # 8 champs, db en UntrackedValue
def initial_state(question: str, db: DuckDBPyConnection) -> AgentState: ...
```

Nodes (agent/nodes.py) — chacun (state) -> dict partiel :
```python
def planner_node(state) -> dict      # {"plan": [...]}
def sql_tool_node(state) -> dict     # {"findings": [...], "iterations": n+1}
def synthesizer_node(state) -> dict  # {"report": "..."}
```

Loader J1 (data/loader.py) :
```python
conn = connect()                              # ":memory:"
tables = load_csvs_to_duckdb(conn, DATA_RAW)  # charge les 9 CSV Olist
```

Config :
```python
from dataagent.config import DATA_RAW   # Path vers data/raw
```

API LangGraph v1.0 :
```python
from langgraph.graph import StateGraph, START, END
g = StateGraph(AgentState)
g.add_node("planner", planner_node)
g.add_edge(START, "planner")
...
app = g.compile()           # obligatoire avant invoke (D-04)
final = app.invoke(state)   # dict final = AgentState
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: build_graph() + run() (graph.py)</name>
  <files>src/dataagent/agent/graph.py</files>
  <read_first>
    - src/dataagent/agent/state.py (AgentState, initial_state)
    - src/dataagent/agent/nodes.py (planner_node, sql_tool_node, synthesizer_node)
    - src/dataagent/data/loader.py (connect, load_csvs_to_duckdb)
    - src/dataagent/config.py (DATA_RAW)
    - .planning/phases/01-state-foundation-minimal-graph/01-CONTEXT.md (D-04, D-05)
  </read_first>
  <action>
    Créer `src/dataagent/agent/graph.py` :
    ```python
    from langgraph.graph import StateGraph, START, END
    from dataagent.agent.state import AgentState, initial_state
    from dataagent.agent.nodes import planner_node, sql_tool_node, synthesizer_node

    def build_graph():
        """Compile le graphe linéaire minimal (D-04, GRAPH-02)."""
        g = StateGraph(AgentState)
        g.add_node("planner", planner_node)
        g.add_node("sql_tool", sql_tool_node)
        g.add_node("synthesizer", synthesizer_node)
        g.add_edge(START, "planner")
        g.add_edge("planner", "sql_tool")
        g.add_edge("sql_tool", "synthesizer")
        g.add_edge("synthesizer", END)
        return g.compile()
    ```
    Ajouter `run(question: str, conn=None) -> dict` :
    - Si `conn is None` : `conn = connect(); load_csvs_to_duckdb(conn, DATA_RAW)` (D-05 : conn créée à l'entrée, chargement Olist via loader J1).
    - `state = initial_state(question, conn)` (injecte db en UntrackedValue dans le state initial — criterion #4).
    - `app = build_graph(); return app.invoke(state)`.
    Le paramètre `conn` optionnel permet de tester avec la fixture mini-Olist (injection) sans recharger les 9 CSV réels.
    Type hints, docstrings courtes. Le compile() est obligatoire avant invoke (D-04).
  </action>
  <acceptance_criteria>
    - `grep -q "StateGraph(AgentState)" src/dataagent/agent/graph.py` réussit
    - `grep -q "add_edge(START" src/dataagent/agent/graph.py` réussit
    - `grep -q "\"synthesizer\", END" src/dataagent/agent/graph.py` réussit
    - `grep -q ".compile()" src/dataagent/agent/graph.py` réussit
    - `python -c "from dataagent.agent.graph import build_graph; build_graph(); print('compiled')"` affiche `compiled`
  </acceptance_criteria>
  <verify>
    <automated>python -c "from dataagent.agent.graph import build_graph, run; app=build_graph(); print(type(app).__name__)"</automated>
  </verify>
  <done>build_graph() compile START->planner->sql_tool->synthesizer->END ; run() crée la conn DuckDB (ou l'accepte), pose le state initial avec db, invoke et retourne le state final.</done>
</task>

<task type="auto">
  <name>Task 2: Entrypoint CLI __main__.py</name>
  <files>src/dataagent/__main__.py</files>
  <read_first>
    - src/dataagent/agent/graph.py (run — task 1)
    - src/dataagent/config.py (GEMINI_API_KEY, DATA_RAW)
    - .planning/phases/01-state-foundation-minimal-graph/01-CONTEXT.md (D-11)
  </read_first>
  <action>
    Créer `src/dataagent/__main__.py` (D-11, GRAPH-06) :
    - `from dotenv import load_dotenv; load_dotenv()` AVANT tout import qui lit la clé (charge GEMINI_API_KEY depuis .env — D-06). Note : config.py lit os.environ au moment de l'import ; donc `load_dotenv()` doit s'exécuter avant `from dataagent.agent.graph import run`. Faire l'import de `run` à l'intérieur de `main()` après load_dotenv, OU load_dotenv en tout début de module avant les imports dataagent.
    - `main()` : parse `sys.argv[1]` comme question (argument positionnel). Si absent : print usage `python -m dataagent "ta question"` et `sys.exit(1)`.
    - Vérifier `GEMINI_API_KEY` présent (sinon message clair "GEMINI_API_KEY manquant dans .env" + exit 1) — fail-fast aux boundaries.
    - Appeler `final = run(question)`, puis `print(final["report"])`.
    - Optionnel : si `--debug` en argv, print aussi `final["plan"]` et `final["iterations"]`/`final["max_iterations"]` (rend max_iterations observable — criterion #3). Garder simple.
    - `if __name__ == "__main__": main()`.
    Pas de secret loggé. Gestion d'erreur aux boundaries (try/except autour de run avec message clair).
  </action>
  <acceptance_criteria>
    - `grep -q "load_dotenv" src/dataagent/__main__.py` réussit
    - `grep -q "sys.argv" src/dataagent/__main__.py` réussit
    - `grep -q "report" src/dataagent/__main__.py` réussit
    - `python -m dataagent` (sans argument) affiche l'usage et exit code != 0
    - `grep -q "GEMINI_API_KEY" src/dataagent/__main__.py` réussit (fail-fast clé)
  </acceptance_criteria>
  <verify>
    <automated>python -m dataagent; test $? -ne 0 && echo "usage-guard-ok"</automated>
  </verify>
  <done>python -m dataagent "question" charge .env, build+invoke le graph, print le report ; sans argument -> usage + exit non nul ; clé manquante -> message clair.</done>
</task>

<task type="auto">
  <name>Task 3: Test end-to-end du graphe (test_graph.py)</name>
  <files>tests/test_graph.py</files>
  <read_first>
    - src/dataagent/agent/graph.py (build_graph, run)
    - src/dataagent/agent/nodes.py (format findings/report)
    - tests/conftest.py (fixture conn mini-Olist — injection)
    - src/dataagent/agent/state.py (AgentState)
  </read_first>
  <action>
    Créer `tests/test_graph.py`. Règle projet : DuckDB réel (fixture `conn` injectée via `run(question, conn=conn)`), LLM mocké (seul mock autorisé). Mock les 3 LLM factory au niveau de `dataagent.agent.nodes` (monkeypatch flash_llm/pro_llm) pour un run déterministe :
    - flash_llm fake : pour le planner renvoie "CA total 2017 ?" ; pour le sql_tool renvoie un SQL valide sur le mini Olist (ex agrégat sur orders/order_items). Astuce : un seul fake qui renvoie un SQL valide générique suffit si le planner produit 1 sous-question.
    - pro_llm fake : renvoie un markdown contenant le mot "orders" (source citée).
    Tests :
    - `test_build_graph_compiles` : `build_graph()` retourne un objet non None (compile OK, D-04).
    - `test_graph_linear_structure` : vérifier les nodes présents via `app.get_graph().nodes` contiennent planner, sql_tool, synthesizer (structure START->planner->sql_tool->synthesizer->END).
    - `test_run_end_to_end_produces_report` (fixture conn) : `final = run("CA total 2017 ?", conn=conn)`. Asserts :
        * `final["plan"]` non vide (criterion #5)
        * `final["report"]` est une str markdown non vide citant une source (criterion #2)
        * `final["findings"]` contient au moins un finding avec une clé `sql`
        * `final["max_iterations"] == 5` et `final["iterations"] >= 1` (criterion #3, observabilité hard stop)
        * `final["db"] is conn` (la conn injectée vit dans le state — criterion #4)
    Si `run` ne supporte pas l'injection de mocks LLM proprement, monkeypatcher `dataagent.agent.nodes.flash_llm` et `.pro_llm` est la voie (les nodes les appellent). Viser couverture ≥80% sur graph.py.
  </action>
  <acceptance_criteria>
    - `grep -q "def test_run_end_to_end" tests/test_graph.py` réussit
    - `python -m pytest tests/test_graph.py -q` sort en code 0
    - Le test e2e asserte `final["max_iterations"] == 5` et `final["db"] is conn`
    - `python -m pytest tests/ -q` (suite complète) sort en code 0 (aucune régression J1)
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/ -q</automated>
  </verify>
  <done>Test e2e vert : graph compilé, run() produit plan[] + report markdown sourcé + finding sql, max_iterations=5 observable, db injectée ; suite complète verte (J1 + agent) ; couverture graph.py ≥80%.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/ -q` passe (J1 loader/queries + state + nodes + graph).
- `python -c "from dataagent.agent.graph import build_graph; build_graph()"` compile sans erreur (D-04).
- `python -m dataagent` sans argument -> usage + exit non nul.
- Run e2e (mock LLM, DuckDB réel) : plan[] non vide, report markdown sourcé, finding sql, max_iterations=5 observable, db is conn.
- Validation manuelle réelle (hors test, nécessite GEMINI_API_KEY) : `python -m dataagent "CA total 2017 ?"` retourne une réponse correcte (criterion #1) — à exécuter par l'utilisateur car appel réseau Gemini.
</verification>

<success_criteria>
- StateGraph compilé câble la boucle linéaire planner->sql_tool->synthesizer (GRAPH-02, criterion).
- `python -m dataagent "question"` invocable end-to-end (GRAPH-06, criterion #1).
- max_iterations observable dans le state final (criterion #3, GRAPH-05).
- db injectée en UntrackedValue dans le state initial (criterion #4, D-05).
- plan[] visible dans le state (criterion #5).
- Suite complète verte, couverture ≥80% sur les nouveaux modules.
</success_criteria>

<output>
After completion, create `.planning/phases/01-state-foundation-minimal-graph/01-03-SUMMARY.md`
</output>
