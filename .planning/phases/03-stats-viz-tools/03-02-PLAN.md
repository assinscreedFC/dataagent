---
phase: 03-stats-viz-tools
plan: "02"
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - src/dataagent/agent/viz.py
  - src/dataagent/agent/nodes.py
  - tests/test_viz.py
autonomous: true
requirements: [TOOL-03]
must_haves:
  truths:
    - "viz_tool produces a plotly PNG file on disk inside config.REPORTS"
    - "The absolute path of the generated PNG is recorded in findings under key png_path"
    - "reports/ is created if absent; PNG filename is deterministic (no timestamp/random)"
  artifacts:
    - path: "src/dataagent/agent/viz.py"
      provides: "render_chart(data, name) -> Path: plotly figure -> PNG via kaleido into config.REPORTS"
      min_lines: 40
      exports: ["render_chart"]
    - path: "src/dataagent/agent/nodes.py"
      provides: "viz_tool_node(state) -> dict pushing finding with png_path"
      contains: "def viz_tool_node"
    - path: "tests/test_viz.py"
      provides: "Real-I/O tests asserting PNG file exists on disk + png_path in finding"
      contains: "def test_"
  key_links:
    - from: "src/dataagent/agent/nodes.py"
      to: "src/dataagent/agent/viz.py"
      via: "import render_chart"
      pattern: "from dataagent.agent.viz import"
    - from: "src/dataagent/agent/viz.py"
      to: "src/dataagent/config.py"
      via: "REPORTS output dir"
      pattern: "REPORTS"
---

<objective>
Build the viz_tool: render a plotly figure from findings data and export it to a deterministic PNG file via kaleido into `config.REPORTS`, then record the absolute PNG path in `findings`. Covers TOOL-03.

Purpose: Give the agent a visualization capability so the downstream synthesizer can reference a chart. No LLM calls — pure render. Deterministic filename = reproducible tests.

Output: `src/dataagent/agent/viz.py` (render_chart pure fn), `viz_tool_node` in `nodes.py`, full test suite in `tests/test_viz.py`.

NOT in scope: router/dispatch (Phase 4), critic loop, checkpointer, ANY change to `graph.py`. The node is built and tested in isolation — NOT wired into the compiled graph this phase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-stats-viz-tools/03-CONTEXT.md

# Code to extend / reference (read_first per task)
@src/dataagent/agent/nodes.py
@src/dataagent/agent/state.py
@src/dataagent/config.py
@tests/conftest.py

<interfaces>
<!-- Contracts the executor needs. Use these directly — no codebase exploration required. -->

Config (src/dataagent/config.py):
```python
REPORTS = PROJECT_ROOT / "reports"   # pathlib.Path, gitignored (reports/*.png)
```

State shape: see Plan 01 interfaces. Node contract: `def node(state: AgentState) -> dict`.
Returning `{"findings": [...]}` APPENDS via the `add` reducer.

sql_tool finding shape (numeric data source for charts):
```python
{"source": "sql_tool", "subquestion": str, "sql": str,
 "tables": [...], "rows": [(...), ...], "columns": [...], "attempts": int}
```

plotly PNG export (verified working with plotly 6.5.2 + kaleido):
```python
import plotly.graph_objects as go
fig = go.Figure(go.Bar(x=[...], y=[...]))   # or go.Scatter(mode="lines") for time-series
fig.write_image(str(path))                  # path = REPORTS / "<deterministic>.png"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: render_chart — plotly figure -> deterministic PNG via kaleido</name>
  <files>src/dataagent/agent/viz.py, tests/test_viz.py</files>
  <read_first>src/dataagent/config.py (REPORTS path), tests/conftest.py (real-I/O test style, tmp usage)</read_first>
  <behavior>
    render_chart(x, y, name, kind="auto"):
    - Test: calling with x=["a","b","c"], y=[1,2,3], name="rev" writes a PNG to REPORTS and returns its absolute Path; the file exists and has size > 0.
    - Test: filename is deterministic — calling twice with the same name returns the same path (slugified name, no timestamp/random), file overwritten not duplicated.
    - Test: REPORTS dir is created if absent (mkdir parents=True, exist_ok=True) — point REPORTS at a tmp dir via monkeypatch and assert it gets created.
    - Test: kind heuristic — numeric/datetime-like x → line (go.Scatter mode="lines"); categorical (str) x → bar. Assert no exception for both; assert PNG produced.
  </behavior>
  <action>
    Create src/dataagent/agent/viz.py with module docstring + a pure render function, type-hinted, PEP8:
    - `render_chart(x: list, y: list, name: str, kind: str = "auto") -> Path`:
      1. `from dataagent.config import REPORTS`; `REPORTS.mkdir(parents=True, exist_ok=True)` (D-07).
      2. Deterministic filename: slugify `name` (lowercase, non-alnum → "_", strip) → `<slug>.png`. NO timestamp, NO random (D-06, reproducibility).
      3. Heuristic (D-06, Claude's discretion on detail): if kind=="auto", choose bar when x values are strings (categorical), else line (go.Scatter mode="lines") for numeric/temporal series. Allow explicit kind="bar"|"line" override.
      4. Build the plotly figure (`import plotly.graph_objects as go`), set a title = name, write via `fig.write_image(str(path))`.
      5. Return the absolute path (`path.resolve()` / `path.absolute()`).
      NO LLM calls, NO randomness.
    Create tests/test_viz.py covering every <behavior> bullet. Use `monkeypatch.setattr("dataagent.agent.viz.REPORTS", tmp_path / "reports")` (or patch config) so PNGs land in a tmp dir — assert file exists, size>0, path absolute, deterministic on repeat. Real kaleido I/O, no mock.
  </action>
  <verify>
    <automated>cd "D:/disc_E/vscode_pyhton/python/dataagent" && python -m pytest tests/test_viz.py -q</automated>
  </verify>
  <done>render_chart writes a real PNG to REPORTS, returns absolute Path; filename deterministic; REPORTS auto-created; bar/line heuristic works; all test_viz.py tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: viz_tool_node — pick data from findings, render, record png_path</name>
  <files>src/dataagent/agent/nodes.py, tests/test_viz.py</files>
  <read_first>src/dataagent/agent/nodes.py (node contract, finding shape), src/dataagent/agent/viz.py (render_chart from Task 1)</read_first>
  <behavior>
    viz_tool_node(state):
    - Test: given findings with a sql_tool result carrying ≥2 rows + ≥2 columns (one label col + one numeric col), node renders a chart and pushes {"source": "viz_tool", "png_path": "<abs path>", "chart": "bar|line", "subquestion": ...}; the png_path file exists on disk.
    - Test: returned dict appends the viz finding (source == "viz_tool") and png_path is an absolute existing path.
    - Test: no chartable finding (no numeric column / <2 rows) → pushes {"source": "viz_tool", "skipped": "no chartable data"} and does NOT crash.
  </behavior>
  <action>
    Add `viz_tool_node(state: AgentState) -> dict` to nodes.py (import `from dataagent.agent.viz import render_chart`).
    Logic (no LLM, pure orchestration):
    1. Read `state.get("findings", [])`. Find the first sql_tool finding with `rows` + `columns` where there is at least one label column (x) and one numeric column (y) and ≥2 rows. Build x from the first column, y from a numeric column (wrap framing/extraction in try/except).
    2. Derive a deterministic name from the finding's `subquestion` (fallback "chart"). Call render_chart(x, y, name) → png_path.
    3. Push {"source": "viz_tool", "png_path": str(png_path), "chart": <kind used or "auto">, "subquestion": subquestion}.
    4. If no chartable finding exists: push {"source": "viz_tool", "skipped": "no chartable data"} (no crash).
    5. Return {"findings": new_findings} (append-only). Do NOT increment iterations (router/critic owns that, Phase 4).
    Add tests to tests/test_viz.py per <behavior>: construct state as a plain dict with a `findings` list; monkeypatch REPORTS to tmp dir; assert png_path in finding and Path(png_path).exists(). Real I/O, no LLM mock, no DuckDB needed.
  </action>
  <verify>
    <automated>cd "D:/disc_E/vscode_pyhton/python/dataagent" && python -m pytest tests/test_viz.py -q && python -c "from dataagent.agent.nodes import viz_tool_node; print('import ok')"</automated>
  </verify>
  <done>viz_tool_node importable; renders PNG from a sql_tool finding and records absolute png_path in a viz_tool finding; skips gracefully on non-chartable data; all tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Coverage check + full regression suite</name>
  <files>tests/test_viz.py</files>
  <read_first>tests/test_viz.py (existing tests from Tasks 1-2)</read_first>
  <action>
    Run the full suite with coverage scoped to viz.py. Add any missing test cases to bring viz.py + viz_tool_node coverage ≥80% (target the "skipped/no chartable data" branch and the bar-vs-line heuristic branches). Do NOT modify production code unless coverage reveals a real bug. Confirm zero regressions across the whole suite (including Plan 01 stats tests).
  </action>
  <verify>
    <automated>cd "D:/disc_E/vscode_pyhton/python/dataagent" && python -m pytest -q && python -m pytest tests/test_viz.py --cov=src/dataagent/agent/viz --cov-report=term-missing -q</automated>
  </verify>
  <done>Full suite green (no regressions). viz.py coverage ≥80%. Both chart-produced and skipped branches exercised.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| findings → viz_tool | viz_tool consumes in-process findings; no external input |
| viz_tool → filesystem (reports/) | PNG written to local REPORTS dir |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Tampering | filename from finding subquestion | mitigate | slugify name (alnum + "_" only) before building path → no path traversal via "../" or separators; fixed .png suffix, writes only inside REPORTS |
| T-03-05 | Denial of Service | render on malformed/huge findings | mitigate | try/except around framing+render; non-chartable data → "skipped" finding, never crashes; data bounded by LIMITed SQL upstream |
| T-03-06 | Information Disclosure | PNG content / path | accept | Chart shows aggregate business numbers only; REPORTS is local + gitignored; no secrets in path |
</threat_model>

<verification>
- `python -m pytest tests/test_viz.py -q` → all pass
- `python -m pytest -q` → full suite green (stats + viz + Phase 1/2), no regressions
- `python -c "from dataagent.agent.viz import render_chart"` → ok
- `python -c "from dataagent.agent.nodes import viz_tool_node"` → ok
- A generated PNG exists on disk and its absolute path appears in a viz_tool finding
- viz.py coverage ≥80%
</verification>

<success_criteria>
1. viz_tool produces a plotly PNG file on disk in config.REPORTS (Success Criterion 3).
2. The absolute PNG path is recorded in findings under key png_path (Success Criterion 4).
3. Filename deterministic (no timestamp/random); reports/ auto-created.
4. graph.py untouched; node built and tested in isolation.
</success_criteria>

<output>
After completion, create `.planning/phases/03-stats-viz-tools/03-02-SUMMARY.md`
</output>
