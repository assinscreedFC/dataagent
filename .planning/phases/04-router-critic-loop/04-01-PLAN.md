---
phase: 04-router-critic-loop
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/dataagent/agent/state.py
  - src/dataagent/agent/nodes.py
  - tests/test_router_critic_nodes.py
autonomous: true
requirements: [TOOL-04, TOOL-05, TOOL-06, TOOL-08]

must_haves:
  truths:
    - "Une fonction de routing renvoie un Literal[\"sql_tool\",\"stats_tool\",\"viz_tool\"] selon la sous-question courante"
    - "critic_node incrémente iterations à CHAQUE passage et avance current_step"
    - "critic_node retourne une décision de suffisance lisible (champ dans le finding ou clé de state)"
    - "synthesizer inclut les findings sql + stats + viz et référence le png_path en image markdown"
    - "current_step existe dans le state, initialisé à 0"
  artifacts:
    - path: "src/dataagent/agent/state.py"
      provides: "Champ current_step + initial_state(current_step=0)"
      contains: "current_step"
    - path: "src/dataagent/agent/nodes.py"
      provides: "route_subquestion (routing fn type-hintée) + critic_node + synthesizer multi-source"
      contains: "def critic_node"
    - path: "tests/test_router_critic_nodes.py"
      provides: "Tests unitaires routing, critic increment, synthesizer multi-source"
      exports: []
  key_links:
    - from: "route_subquestion"
      to: "state['plan'] via state['current_step']"
      via: "indexation de la sous-question courante"
      pattern: "plan\\[.*current_step"
    - from: "critic_node"
      to: "state['iterations']"
      via: "increment systématique"
      pattern: "iterations.*\\+\\s*1"
---

<objective>
Ajouter au state le champ `current_step` (index de la sous-question courante) et créer dans `nodes.py` les deux briques de la boucle agentique : la fonction de routing type-hintée (`route_subquestion`, retour `Literal["sql_tool","stats_tool","viz_tool"]`) et le `critic_node` (flash, juge la suffisance, incrémente toujours `iterations`, avance `current_step`). Étendre `synthesizer_node` au multi-source (sql + stats + viz, png_path en image markdown). Aucun câblage de graphe ici (Plan 02). Ne PAS casser les nodes existants ni leurs tests.

Purpose: Fournir les contrats (routing fn + critic) que le Plan 02 branchera dans le graphe via `add_conditional_edges` + `path_map`. Couvre TOOL-04 (routing fn), TOOL-05 (critic), TOOL-06 (increment systématique = base du hard cap), TOOL-08 (synthesizer multi-source).
Output: state.py étendu, nodes.py avec route_subquestion + critic_node + synthesizer multi-source, tests unitaires.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-router-critic-loop/04-CONTEXT.md
@PLAN.md
@src/dataagent/agent/state.py
@src/dataagent/agent/nodes.py
@src/dataagent/agent/llm.py
@src/dataagent/config.py
@tests/conftest.py

<interfaces>
<!-- Contrats existants — utiliser tels quels, pas d'exploration nécessaire -->

State (src/dataagent/agent/state.py) :
```python
class AgentState(TypedDict):
    question: str
    plan: list[str]
    findings: Annotated[list[dict], add]      # reducer add → append automatique
    messages: Annotated[list, add_messages]
    iterations: int
    max_iterations: int
    db: Annotated[DuckDBPyConnection, UntrackedValue(DuckDBPyConnection)]
    report: str
# initial_state(question, db) -> AgentState (iterations=0, max_iterations=MAX_ITERATIONS)
```

LLM (src/dataagent/agent/llm.py) :
```python
def flash_llm() -> ChatGoogleGenerativeAI   # planner/router/critic
def pro_llm()  -> ChatGoogleGenerativeAI    # synthesizer
# Dans nodes.py : appeler flash_llm() / pro_llm() (déjà importés) — les tests monkeypatch
#   nodes.flash_llm / nodes.pro_llm (lambda: _FakeLLM(...)). NE PAS importer ailleurs.
```

Findings produits par les tools existants (nodes.py) :
```python
# sql_tool   : {"source":"sql_tool","subquestion","sql","tables","rows","columns","attempts"}  (ou {...,"error"})
# stats_tool : {"source":"stats_tool","analysis":"correlation"|"anomaly"|"insufficient_data", ...}
# viz_tool   : {"source":"viz_tool","subquestion","png_path","chart"}  (ou {"source":"viz_tool","skipped":...})
```

config (src/dataagent/config.py) : MAX_ITERATIONS = 5
</interfaces>

<routing_contract>
<!-- Heuristique de routing (D-02, Claude's Discretion) -->
Primaire : mots-clés sur la sous-question courante (déterministe, testable) :
- corrélation / corrélé / anomalie / aberrant / écart-type / tendance → "stats_tool"
- graphe / visualise / courbe / histogramme / chart / plot / diagramme → "viz_tool"
- sinon → "sql_tool"
Le retour DOIT être typé `Literal["sql_tool","stats_tool","viz_tool"]`.
La sous-question courante = `state["plan"][state["current_step"]]` avec garde si index hors borne → "sql_tool".
(Optionnel/discrétion : appel flash de classification ; si réponse hors enum → fallback mots-clés.
 Garder le déterminisme testable : le test mocke flash_llm si utilisé.)
</routing_contract>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ajouter current_step au state</name>
  <files>src/dataagent/agent/state.py</files>
  <read_first>src/dataagent/agent/state.py (intégralité — 49 lignes)</read_first>
  <action>Ajouter le champ `current_step: int` à `AgentState` (après `max_iterations`, avant `db`). Dans `initial_state()`, initialiser `current_step=0`. Conserver tous les champs existants à l'identique (D-07). Docstring : "current_step = index de la sous-question courante dans plan[], avancé par le critic à chaque tour de boucle." Ne PAS toucher au reducer findings ni à db UntrackedValue.</action>
  <verify>
    <automated>python -c "from dataagent.agent.state import AgentState, initial_state; import duckdb; s=initial_state('q', duckdb.connect()); assert s['current_step']==0; assert 'current_step' in AgentState.__annotations__; print('OK')"</automated>
  </verify>
  <done>`current_step` présent dans AgentState.__annotations__ et initialisé à 0 par initial_state ; tests existants (test_state.py) toujours verts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: route_subquestion (routing fn type-hintée) + critic_node</name>
  <files>src/dataagent/agent/nodes.py, tests/test_router_critic_nodes.py</files>
  <read_first>src/dataagent/agent/nodes.py (planner_node, sql_tool_node, synthesizer_node, imports flash_llm/pro_llm) ; tests/conftest.py (fixture conn) ; tests/test_graph.py (pattern _FakeLLM/_FakeResponse + monkeypatch.setattr(nodes, "flash_llm", ...))</read_first>
  <behavior>
    - route_subquestion(state) renvoie "stats_tool" si la sous-question courante contient un mot-clé stats (corrélation/anomalie/écart-type/tendance)
    - route_subquestion(state) renvoie "viz_tool" si mot-clé viz (graphe/visualise/courbe/chart/plot)
    - route_subquestion(state) renvoie "sql_tool" sinon (défaut)
    - route_subquestion garde-fou : si current_step >= len(plan) → "sql_tool" (pas d'IndexError)
    - route_subquestion : return type-hinté Literal["sql_tool","stats_tool","viz_tool"]
    - critic_node incrémente iterations de 1 EXACTEMENT à chaque appel (state["iterations"]+1)
    - critic_node avance current_step de 1 (state["current_step"]+1)
    - critic_node retourne une décision de suffisance déterministe quand flash est mocké (clé exploitable par le conditional edge du Plan 02, ex: dernier élément de findings {"source":"critic","sufficient":bool} OU une clé de state dédiée — choisir UNE convention et la documenter)
  </behavior>
  <action>
    1. Écrire d'abord tests/test_router_critic_nodes.py (RED) couvrant les behaviors ci-dessus. Réutiliser _FakeResponse/_FakeLLM du pattern test_graph.py et monkeypatch.setattr(nodes, "flash_llm", lambda: _FakeFlash(...)). Mocker flash pour que le critic réponde "SUFFISANT"/"INSUFFISANT" de façon contrôlée.
    2. Implémenter `route_subquestion(state: AgentState) -> Literal["sql_tool","stats_tool","viz_tool"]` selon <routing_contract> (mots-clés sur state["plan"][state["current_step"]], garde index, lowercase pour le matching). Importer Literal depuis typing.
    3. Implémenter `critic_node(state: AgentState) -> dict` : appelle flash_llm() pour juger si les findings accumulés répondent à state["question"] (prompt court : "Réponds SUFFISANT ou INSUFFISANT"). Parse insensible à la casse. TOUJOURS retourner iterations=state["iterations"]+1 et current_step=state["current_step"]+1 (D-04, D-06). Pousser un finding {"source":"critic","sufficient":bool,"iteration":...} dans findings (reducer add) — c'est la convention que le Plan 02 lira. Le critic NE doit jamais boucler lui-même : la décision de reloop est l'affaire du conditional edge (Plan 02).
    Choix de convention décision : exposer la suffisance via le DERNIER finding source="critic" (clé "sufficient": bool). Documenter ce contrat en docstring pour le Plan 02.
  </action>
  <verify>
    <automated>python -m pytest tests/test_router_critic_nodes.py -q</automated>
  </verify>
  <done>route_subquestion route correctement les 3 types de sous-questions + garde index ; critic_node incrémente iterations de 1 et current_step de 1 à chaque appel et pousse un finding source="critic" avec "sufficient" bool ; tests verts.</done>
</task>

<task type="auto">
  <name>Task 3: synthesizer multi-source (sql + stats + viz + image png)</name>
  <files>src/dataagent/agent/nodes.py, tests/test_router_critic_nodes.py</files>
  <read_first>src/dataagent/agent/nodes.py (_serialize_findings + synthesizer_node lignes 232-253 et 408-433)</read_first>
  <action>Étendre `_serialize_findings` (ne PAS le réécrire entièrement — ajouter le traitement des sources stats_tool et viz_tool en plus du sql_tool existant, D-08) : pour un finding source="stats_tool" sérialiser analysis + value/anomalies ; pour source="viz_tool" avec png_path, sérialiser le chemin ET signaler au prompt qu'une image existe. Étendre le system prompt du synthesizer pour : (a) croiser sql+stats+viz, (b) si un finding viz a un png_path, INCLURE une image markdown `![graphe](png_path)` dans le rapport. Ignorer/résumer les findings source="critic" (ne pas les présenter comme data). Conserver le comportement existant pour les findings sql et le cas findings vides. Ajouter au fichier test un test synthesizer multi-source : findings mêlant sql + stats + viz (avec png_path), pro_llm mocké pour renvoyer un rapport contenant `![`, asserter que le report inclut une image markdown et référence les 3 sources via le findings_text passé au prompt.</action>
  <verify>
    <automated>python -m pytest tests/test_router_critic_nodes.py -q</automated>
  </verify>
  <done>_serialize_findings traite stats_tool + viz_tool ; le prompt synthesizer demande l'inclusion image markdown quand png_path présent ; test multi-source vert ; test_graph.py (synthesizer sql-only) toujours vert (pas de régression).</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/ -q` : 0 régression (nodes existants + graph linéaire encore verts à ce stade)
- `python -m pytest tests/test_router_critic_nodes.py --cov=dataagent.agent.nodes --cov-report=term-missing` : couverture des nouvelles fns ≥ 80%
- route_subquestion : return type `Literal["sql_tool","stats_tool","viz_tool"]` (grep `Literal\[` dans nodes.py)
- critic_node : increment iterations vérifié par test
</verification>

<success_criteria>
1. current_step dans le state, initialisé à 0 (D-07)
2. route_subquestion type-hintée Literal route sql/stats/viz selon la sous-question courante (TOOL-04, base)
3. critic_node incrémente toujours iterations + avance current_step + expose "sufficient" (TOOL-05, TOOL-06 base)
4. synthesizer multi-source inclut image markdown du png (TOOL-08 base)
5. Aucune régression sur les tests existants
</success_criteria>

<output>
Après complétion, créer `.planning/phases/04-router-critic-loop/04-01-SUMMARY.md`
</output>
