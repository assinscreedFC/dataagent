---
phase: 04-router-critic-loop
plan: "02"
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/dataagent/agent/graph.py
  - src/dataagent/agent/nodes.py
  - tests/test_critic_loop.py
autonomous: true
requirements: [TOOL-04, TOOL-05, TOOL-06, TOOL-08]

must_haves:
  truths:
    - "build_graph() construit le graphe branché START->planner->router-(cond)->{sql,stats,viz}->critic-(cond)->{router|synthesizer}->END"
    - "Le conditional edge du router utilise un path_map explicite mappant chaque Literal vers le node tool"
    - "Le conditional edge du critic reboucle vers router si insuffisant ET iterations<max_iterations, sinon synthesizer (path_map explicite)"
    - "Avec un critic mocké TOUJOURS insuffisant, la boucle s'arrête EXACTEMENT à max_iterations (pas de boucle infinie)"
    - "Une question complexe produit un report markdown multi-source incluant une image de graphe"
  artifacts:
    - path: "src/dataagent/agent/graph.py"
      provides: "build_graph() branché avec add_conditional_edges + path_map (router et critic)"
      contains: "path_map"
    - path: "tests/test_critic_loop.py"
      provides: "Tests intégration : routing correct, reloop puis synth, HARD CAP, multi-source report"
      exports: []
  key_links:
    - from: "build_graph router edge"
      to: "route_subquestion via add_conditional_edges"
      via: "path_map={'sql_tool':'sql_tool','stats_tool':'stats_tool','viz_tool':'viz_tool'}"
      pattern: "add_conditional_edges.*router"
    - from: "build_graph critic edge"
      to: "synthesizer ou router selon iterations<max_iterations"
      via: "fonction de décision + path_map"
      pattern: "add_conditional_edges.*critic"
---

<objective>
Restructurer `build_graph()` (graph.py) du linéaire vers le graphe agentique branché conforme au schéma PLAN.md, en utilisant `add_conditional_edges` avec `path_map` EXPLICITE (exigence dure : sans path_map = misroute silencieux). Câbler : planner -> router (cond, route_subquestion du Plan 01) -> {sql_tool|stats_tool|viz_tool} -> critic -> (cond) -> {router si insuffisant ET iterations<max_iterations | synthesizer sinon} -> END. Conserver `run()`. Écrire les tests d'intégration de la boucle, dont le test HARD CAP anti-boucle-infinie.

Purpose: Couvre TOOL-04 (router conditional edge + path_map), TOOL-05 (critic reboucle/synthétise), TOOL-06 (hard cap max_iterations), TOOL-08 (rapport multi-source avec graphe end-to-end via le graphe).
Output: graph.py branché, ajustement iterations dans sql_tool_node, tests/test_critic_loop.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-router-critic-loop/04-CONTEXT.md
@.planning/phases/04-router-critic-loop/04-01-SUMMARY.md
@PLAN.md
@src/dataagent/agent/graph.py
@src/dataagent/agent/nodes.py
@src/dataagent/config.py
@tests/conftest.py
@tests/test_graph.py

<interfaces>
<!-- LangGraph v1.0 — API vérifiée sur l'install locale -->
```python
StateGraph.add_conditional_edges(
    source: str,
    path: Callable[..., Hashable],          # route_subquestion / décision critic
    path_map: dict[Hashable, str] | None,   # OBLIGATOIRE ici (anti-misroute)
)
# La fonction `path` reçoit le state et retourne une clé ; path_map mappe clé -> nom de node.
```

Fournis par le Plan 01 (déjà implémentés, à importer depuis nodes) :
```python
route_subquestion(state) -> Literal["sql_tool","stats_tool","viz_tool"]
critic_node(state) -> dict   # incrémente iterations+1, current_step+1, pousse finding {"source":"critic","sufficient":bool}
# state porte désormais current_step (init 0)
```

Nodes existants : planner_node, sql_tool_node, stats_tool_node, viz_tool_node, synthesizer_node.
config : MAX_ITERATIONS = 5.

run() actuel (à conserver) : crée conn si None, initial_state, build_graph().invoke(state).
```
</interfaces>

<graph_target>
```
START -> planner -> router -+-> sql_tool ---+
                            +-> stats_tool -+-> critic -> (cond) -+-> router (reloop si insufficient ET iterations<max)
                            +-> viz_tool ---+                     +-> synthesizer -> END
```
Notes câblage :
- "router" : pas un node lourd — c'est add_conditional_edges sur un node pivot. Implémenter "router" comme node pass-through (retourne {} ou state inchangé) PUIS add_conditional_edges("router", route_subquestion, path_map={"sql_tool":"sql_tool","stats_tool":"stats_tool","viz_tool":"viz_tool"}). Edge START->planner, planner->router.
- Chaque tool -> "critic" (add_edge).
- critic : add_conditional_edges("critic", _critic_decision, path_map={"router":"router","synthesizer":"synthesizer"}). synthesizer->END.
- _critic_decision(state): "router" si (dernier finding critic non sufficient) ET state["iterations"] < state["max_iterations"] ; sinon "synthesizer". (TOOL-06 hard cap.)
</graph_target>
</context>

<tasks>

<task type="auto">
  <name>Task 1: iterations possédé par le critic (retirer l'incrément du sql_tool)</name>
  <files>src/dataagent/agent/nodes.py</files>
  <read_first>src/dataagent/agent/nodes.py (sql_tool_node lignes 203-224) ; .planning/phases/04-router-critic-loop/04-01-SUMMARY.md (convention iterations du critic)</read_first>
  <action>Dans le graphe branché, le critic est le SEUL à incrémenter iterations (D-06). Or sql_tool_node incrémente actuellement iterations (héritage Phase 1, ligne 223). Retirer `"iterations": state["iterations"] + 1` du return de sql_tool_node — il ne renvoie plus que {"findings": findings}. Cohérent avec stats_tool_node/viz_tool_node qui n'incrémentent déjà pas (cf. SUMMARYs Phase 3). Mettre à jour la docstring de sql_tool_node ("n'incrémente plus iterations — possédé par le critic en Phase 4"). ATTENTION régression : test_graph.py asserte iterations>=1 après run() linéaire ; ce test devient incohérent avec le nouveau graphe — il sera remplacé/ajusté en Task 3 (le graphe branché passe forcément par le critic ≥1 fois donc iterations>=1 reste vrai end-to-end). Ne PAS modifier test_graph.py ici si l'assertion iterations>=1 passe encore end-to-end via le critic ; sinon l'ajuster en Task 3.</action>
  <verify>
    <automated>python -c "import inspect; from dataagent.agent import nodes; src=inspect.getsource(nodes.sql_tool_node); assert 'iterations' not in src.split('return')[-1], 'sql_tool_node retourne encore iterations'; print('OK')"</automated>
  </verify>
  <done>sql_tool_node ne retourne plus iterations ; seul critic_node l'incrémente.</done>
</task>

<task type="auto">
  <name>Task 2: build_graph() branché avec path_map (router + critic)</name>
  <files>src/dataagent/agent/graph.py</files>
  <read_first>src/dataagent/agent/graph.py (intégralité) ; src/dataagent/agent/nodes.py (route_subquestion, critic_node — signatures)</read_first>
  <action>
    Restructurer `build_graph()` selon <graph_target>. Importer route_subquestion, critic_node, stats_tool_node, viz_tool_node depuis nodes (en plus de planner/sql_tool/synthesizer).
    1. Ajouter un node "router" pass-through : `def _router_node(state): return {}` (défini dans graph.py ou réutiliser un no-op) — sert de point d'ancrage pour le conditional edge.
    2. add_node pour : planner, router, sql_tool, stats_tool, viz_tool, critic, synthesizer.
    3. Edges : START->planner ; planner->router.
    4. add_conditional_edges("router", route_subquestion, path_map={"sql_tool":"sql_tool","stats_tool":"stats_tool","viz_tool":"viz_tool"}). path_map OBLIGATOIRE (D-01, anti-misroute silencieux).
    5. add_edge de chaque tool ("sql_tool","stats_tool","viz_tool") -> "critic".
    6. Définir `_critic_decision(state) -> Literal["router","synthesizer"]` : lit le dernier finding source="critic" (clé "sufficient") ; retourne "synthesizer" si sufficient OU state["iterations"] >= state["max_iterations"] (HARD CAP, D-06), sinon "router". Type-hinter le retour Literal.
    7. add_conditional_edges("critic", _critic_decision, path_map={"router":"router","synthesizer":"synthesizer"}).
    8. add_edge("synthesizer", END). compile() et return.
    Conserver `run()` à l'identique (il appelle build_graph().invoke(state) ; initial_state fournit déjà current_step=0 via Plan 01).
  </action>
  <verify>
    <automated>python -c "from dataagent.agent.graph import build_graph; app=build_graph(); ns=app.get_graph().nodes; assert all(n in ns for n in ['planner','router','sql_tool','stats_tool','viz_tool','critic','synthesizer']), ns; print('OK', list(ns))"</automated>
  </verify>
  <done>build_graph() compile le graphe branché ; les 7 nodes présents ; deux add_conditional_edges avec path_map explicite (router + critic) ; run() inchangé.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Tests intégration boucle critic — routing, reloop, HARD CAP, multi-source</name>
  <files>tests/test_critic_loop.py, tests/test_graph.py</files>
  <read_first>tests/test_graph.py (pattern _FakeLLM/_FakeResponse + monkeypatch.setattr(nodes, "flash_llm"/"pro_llm")) ; tests/conftest.py (fixture conn) ; src/dataagent/config.py (MAX_ITERATIONS)</read_first>
  <behavior>
    - (a) ROUTING : une sous-question "stats" est routée vers stats_tool ; une "viz" vers viz_tool ; une simple vers sql_tool (asserter via route_subquestion sur des states construits OU via les findings sources produits end-to-end)
    - (b) RELOOP puis SYNTH : critic mocké INSUFFISANT 1 fois puis SUFFISANT → le run produit un report ET iterations==2 (un reloop puis synth)
    - (c) HARD CAP (CRITIQUE) : critic mocké TOUJOURS "INSUFFISANT" → run() TERMINE (pas de hang), final["iterations"] == max_iterations (==5), un report est quand même produit. Asserter EXACTEMENT iterations==final["max_iterations"] et que le graphe ne boucle pas indéfiniment (le test doit terminer — utiliser un compteur d'appels flash borné pour prouver l'arrêt).
    - (d) MULTI-SOURCE : un plan avec sous-questions sql+stats+viz, pro mocké renvoyant un report avec `![` → final["report"] contient une image markdown ET findings contient des sources sql_tool + stats_tool + viz_tool.
  </behavior>
  <action>
    1. Écrire tests/test_critic_loop.py (RED d'abord). Réutiliser _FakeResponse/_FakeLLM. Construire des _FakeFlash multi-tours : différencier planner vs sql-gen vs critic selon l'ordre d'appel OU selon le contenu du message (le critic prompt contient "SUFFISANT/INSUFFISANT"). Pour le HARD CAP : _FakeFlash dont l'appel critic renvoie TOUJOURS "INSUFFISANT" ; asserter final["iterations"]==final["max_iterations"]. NB anti-récursion LangGraph : si LangGraph lève GraphRecursionError avant le hard cap applicatif, augmenter recursion_limit à l'invoke OU s'assurer que _critic_decision coupe bien à iterations>=max_iterations (le hard cap APPLICATIF doit arrêter avant la limite LangGraph). Vérifier que MAX_ITERATIONS(5) < recursion_limit par défaut (25) — donc le hard cap applicatif s'arrête en premier ; documenter.
    2. Implémenter le strict nécessaire pour que (a)-(d) passent (le code des Plans 01/02 doit déjà suffire — ces tests valident l'intégration).
    3. Ajuster tests/test_graph.py : l'ancien test_graph_linear_structure asserte une structure linéaire qui n'existe plus → mettre à jour pour le graphe branché (vérifier présence des 7 nodes) sans casser test_run_end_to_end_produces_report (qui doit encore passer end-to-end via la nouvelle boucle ; adapter le _FakeFlash pour inclure une réponse critic SUFFISANT afin de sortir au 1er tour). Conserver les assertions criterion #2/#3/#4/#5.
  </action>
  <verify>
    <automated>python -m pytest tests/test_critic_loop.py tests/test_graph.py -q</automated>
  </verify>
  <done>Les 4 cas (a routing, b reloop+synth, c HARD CAP iterations==max_iterations sans hang, d multi-source avec image) passent ; test_graph.py ajusté au graphe branché sans régression ; suite complète verte.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/ -q` : suite complète verte, 0 régression
- `python -m pytest tests/test_critic_loop.py --cov=dataagent.agent.graph --cov=dataagent.agent.nodes --cov-report=term-missing` : couverture graph.py + boucle critic ≥ 80%
- grep `path_map` dans graph.py : présent sur les 2 add_conditional_edges (router + critic)
- HARD CAP : test (c) prouve l'arrêt à max_iterations (pas de boucle infinie), terminaison du test = preuve
</verification>

<success_criteria>
1. Router conditional edge type-hinté + path_map route chaque sous-question vers sql/stats/viz (TOOL-04) — Success Criteria #1
2. Critic juge, reboucle si insuffisant, incrémente iterations (TOOL-05) — Success Criteria #2
3. Boucle s'arrête à max_iterations même critic insatisfait, pas de boucle infinie (TOOL-06) — Success Criteria #3
4. Question complexe -> report markdown multi-source incluant un graphe (TOOL-08) — Success Criteria #4
</success_criteria>

<output>
Après complétion, créer `.planning/phases/04-router-critic-loop/04-02-SUMMARY.md`
</output>
