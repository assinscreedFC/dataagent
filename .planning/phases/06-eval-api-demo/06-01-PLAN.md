---
phase: 06-eval-api-demo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/dataagent/eval/__init__.py
  - src/dataagent/eval/dataset.py
  - src/dataagent/eval/runner.py
  - tests/test_eval.py
autonomous: false
requirements: [EVAL-01]
must_haves:
  truths:
    - "A dataset of 10 {question, expected_keywords} entries on Olist exists and is importable"
    - "The runner executes the agent per question and scores correctness as the matched-keyword ratio in the report"
    - "An aggregate score across all questions is produced"
    - "The harness is fully testable with a MOCKED agent (no Gemini quota consumed)"
  artifacts:
    - path: "src/dataagent/eval/dataset.py"
      provides: "QUESTIONS list of 10 {question, expected_keywords}"
      contains: "QUESTIONS"
      min_lines: 25
    - path: "src/dataagent/eval/runner.py"
      provides: "score_report() + run_eval() returning per-question + aggregate score"
      exports: ["score_report", "run_eval"]
      min_lines: 30
    - path: "tests/test_eval.py"
      provides: "Tests with mocked run() — dataset size, scoring math, aggregate"
      contains: "def test_"
  key_links:
    - from: "src/dataagent/eval/runner.py"
      to: "dataagent.agent.graph.run"
      via: "import + call per question"
      pattern: "from dataagent.agent.graph import run|graph\\.run"
    - from: "src/dataagent/eval/runner.py"
      to: "src/dataagent/eval/dataset.py"
      via: "imports QUESTIONS"
      pattern: "QUESTIONS"
---

<objective>
Build the eval harness (EVAL-01): a versioned dataset of 10 business questions on Olist with expected keywords, plus a runner that executes the agent per question and scores correctness by keyword-presence ratio in the generated report, producing per-question and aggregate scores.

Purpose: Measure agent correctness in a reproducible, quota-free way. The scoring logic and harness are proven by mocked tests; the real 10-question live run is a separate manual/human-verify item (D-03) and is NOT part of CI.
Output: `src/dataagent/eval/` module (dataset + runner) and `tests/test_eval.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-eval-api-demo/06-CONTEXT.md

@src/dataagent/agent/graph.py
@tests/conftest.py

<interfaces>
<!-- Contracts the executor needs — use directly, no codebase exploration required. -->

From src/dataagent/agent/graph.py:
```python
def run(question: str, conn=None, thread_id: str | None = None) -> dict:
    # Returns final AgentState (dict) with keys: question, plan, findings,
    # iterations, max_iterations, current_step, report.
    # report: str — the markdown report.
    # findings: list[dict] — each dict has "source"; viz findings carry "png_path": str.
```

Mock pattern (from tests/test_graph.py): the agent's LLMs are normally mocked via
`monkeypatch.setattr(nodes, "flash_llm", ...)` / `pro_llm`. For EVAL tests, do NOT
re-mock the LLM internals — instead patch the `run` symbol that runner.py imports, so
the runner receives a deterministic fake report. This keeps the eval test independent
of the agent internals and consumes zero quota.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Eval dataset — 10 Olist questions with expected keywords</name>
  <files>src/dataagent/eval/__init__.py, src/dataagent/eval/dataset.py</files>
  <read_first>src/dataagent/agent/graph.py (run signature), .planning/phases/06-eval-api-demo/06-CONTEXT.md (D-01)</read_first>
  <action>
    Create `src/dataagent/eval/__init__.py` (empty package marker).
    Create `src/dataagent/eval/dataset.py` defining `QUESTIONS: list[dict]` with exactly 10
    entries (D-01). Each entry: `{"question": str, "expected_keywords": list[str]}`.
    Use realistic Olist business questions (CA/revenu, catégories, délais de livraison,
    avis/review scores, freight, top produits). expected_keywords = lowercase tokens/table
    names/order-of-magnitude terms expected in a correct report (e.g. for a revenue question:
    ["chiffre", "order_items", "price"]; for delays: ["livraison", "orders", "jours"]).
    Keep keywords matchable case-insensitively against report text. Type-hint the module-level
    QUESTIONS constant. Add a one-line module docstring.
    Per D-01: this is a versioned dataset file (committed), Python list (chosen over JSON for
    type-hints + import simplicity, Claude's Discretion per CONTEXT).
  </action>
  <verify>
    <automated>python -c "from dataagent.eval.dataset import QUESTIONS; assert len(QUESTIONS)==10; assert all('question' in q and 'expected_keywords' in q and len(q['expected_keywords'])>=1 for q in QUESTIONS); print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `from dataagent.eval.dataset import QUESTIONS` works
    - `len(QUESTIONS) == 10`
    - Every entry has non-empty `question` (str) and `expected_keywords` (list with >=1 item)
  </acceptance_criteria>
  <done>dataset.py exports QUESTIONS with 10 well-formed {question, expected_keywords} entries.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Eval runner — score_report + run_eval (mocked-testable)</name>
  <files>src/dataagent/eval/runner.py, tests/test_eval.py</files>
  <read_first>src/dataagent/eval/dataset.py, src/dataagent/agent/graph.py, tests/conftest.py</read_first>
  <behavior>
    - score_report(report, expected_keywords) returns matched/total ratio in [0.0, 1.0], case-insensitive substring match
    - score_report with all keywords present returns 1.0
    - score_report with no keywords present returns 0.0
    - score_report with 1 of 2 present returns 0.5
    - score_report with empty expected_keywords returns 0.0 (or 1.0 — pick one, no ZeroDivisionError)
    - run_eval(conn, run_fn) iterates QUESTIONS, calls run_fn(question, conn=conn), scores each report, returns {"per_question": [{question, score}], "aggregate": float}
    - aggregate == mean of per-question scores
    - run_eval test uses a fake run_fn returning a known report — NO real run() call, NO Gemini
  </behavior>
  <action>
    Create `src/dataagent/eval/runner.py`:
    - `score_report(report: str, expected_keywords: list[str]) -> float`: ratio of expected_keywords
      found (case-insensitive substring) in report. Guard empty keyword list (return 0.0, no div-by-zero).
    - `run_eval(conn=None, run_fn=run) -> dict`: import `run` from `dataagent.agent.graph` as the
      DEFAULT `run_fn` (dependency injection so tests pass a fake). For each entry in QUESTIONS,
      call `run_fn(entry["question"], conn=conn)`, read `result.get("report", "")`, score it,
      collect `{"question", "score"}`. Return `{"per_question": [...], "aggregate": mean_score, "n": 10}`.
    - Keep it pure/synchronous. No print of secrets. Type-hint all signatures.
    Create `tests/test_eval.py`:
    - Test score_report math: 1.0 (all present), 0.0 (none), 0.5 (1/2), empty-keywords guard.
    - Test run_eval with a fake `run_fn=lambda q, conn=None: {"report": "<deterministic text containing some keywords>"}`
      → assert aggregate is a float in [0,1], per_question has 10 entries, aggregate equals mean.
    - DO NOT call the real agent or any LLM (D-03 — zero quota). Use the `conn` fixture only if
      a non-None conn is needed for signature; the fake run_fn ignores it.
    Per D-02/D-03: the harness is fully proven by mocked tests; live run is out of CI.
  </action>
  <verify>
    <automated>python -m pytest tests/test_eval.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "def score_report" src/dataagent/eval/runner.py` and `grep -q "def run_eval" src/dataagent/eval/runner.py`
    - `pytest tests/test_eval.py` passes
    - No test imports/triggers a real Gemini call (fake run_fn used)
    - `python -m pytest --cov=src/dataagent/eval tests/test_eval.py` shows >=80% on eval module
  </acceptance_criteria>
  <done>runner.py exports score_report + run_eval; tests pass with a mocked run_fn; coverage >=80% on eval/.</done>
</task>

<task type="checkpoint:human-verify" gate="non-blocking">
  <name>Task 3: Live 10-question eval run (manual sample, NOT CI-blocking)</name>
  <action>MANUAL human-verify — no autonomous code. Follow the how-to-verify steps to run a sampled live eval.</action>
  <what-built>
    A versioned 10-question Olist dataset and a runner (`run_eval`) that scores agent correctness
    by keyword-presence ratio. The harness + scoring are proven by mocked tests (CI green without
    quota). This checkpoint covers the REAL live run, which consumes Gemini free-tier quota (D-03).
  </what-built>
  <how-to-verify>
    Optional / sampled (do NOT block the milestone on full quota):
    1. Ensure GEMINI_API_KEY is set and Olist CSVs are in data/raw/.
    2. Run a SAMPLE (1-3 questions, not all 10, to respect free-tier quota):
       `python -c "from dataagent.eval.runner import run_eval; from dataagent.eval.dataset import QUESTIONS; QUESTIONS[:] = QUESTIONS[:2]; import json; print(json.dumps(run_eval(), indent=2))"`
    3. Confirm each question returns a report and a per-question score, and an aggregate score prints.
    4. Note the aggregate score for the Labs writeup.
  </how-to-verify>
  <resume-signal>Type "approved" with the observed aggregate score, or "skipped — quota" to proceed without the live run.</resume-signal>
</task>

</tasks>

<verification>
- `from dataagent.eval.dataset import QUESTIONS` yields 10 well-formed entries.
- `pytest tests/test_eval.py` passes with a mocked run_fn (no Gemini).
- `score_report` and `run_eval` exported and type-hinted.
- Aggregate score is the mean of per-question scores.
- Live run is a non-blocking human-verify item (D-03).
</verification>

<success_criteria>
EVAL-01 satisfied: a set of 10 test questions runs (via run_eval) and reports a correctness score.
The scoring harness is proven by quota-free mocked tests; the live run is sampled/manual.
</success_criteria>

<output>
After completion, create `.planning/phases/06-eval-api-demo/06-01-SUMMARY.md`.
</output>
