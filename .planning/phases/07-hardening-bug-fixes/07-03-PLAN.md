---
phase: 07-hardening-bug-fixes
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/dataagent/agent/llm.py
autonomous: true
requirements: [HARD-10]
must_haves:
  truths:
    - "flash_llm() retourne le même objet à chaque appel (singleton module-level)"
    - "pro_llm() retourne le même objet à chaque appel (singleton module-level)"
    - "Le monkeypatch de flash_llm/pro_llm dans les tests reste valide (les tests patchent la fonction, pas le cache)"
  artifacts:
    - path: "src/dataagent/agent/llm.py"
      provides: "singletons _flash/_pro créés au 1er appel"
      contains: "_flash"
  key_links:
    - from: "src/dataagent/agent/llm.py:flash_llm"
      to: "_flash module-level cache"
      via: "création paresseuse au 1er appel"
      pattern: "global _flash"
---

<objective>
Transformer `flash_llm()`/`pro_llm()` en singletons module-level pour éliminer la ré-instanciation ~30×/run du client Gemini.

Purpose: HARD-10 (perf). Plan isolé sur `llm.py` (aucun overlap fichier avec 07-02) → s'exécute en wave 2 en parallèle de 07-02.
Output: llm.py avec cache paresseux.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-hardening-bug-fixes/07-CONTEXT.md
@src/dataagent/agent/llm.py

<interfaces>
llm.py (actuel) :
  def flash_llm() -> ChatGoogleGenerativeAI   # ~16 ; ré-instancie à chaque appel
  def pro_llm() -> ChatGoogleGenerativeAI     # ~25 ; ré-instancie à chaque appel
  NB : nodes.py appelle flash_llm()/pro_llm() ; les tests monkeypatchent ces noms
       (monkeypatch.setattr(nodes, "flash_llm", lambda: _FakeLLM(...))) — le patch
       remplace la FONCTION, donc le cache singleton est transparent pour les tests.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Singletons module-level flash_llm/pro_llm (HARD-10)</name>
  <files>src/dataagent/agent/llm.py</files>
  <read_first>llm.py (entier)</read_first>
  <behavior>
    - flash_llm() is flash_llm()  (même objet retourné aux appels successifs)
    - pro_llm() is pro_llm()      (idem)
    - flash_llm() is not pro_llm() (deux instances distinctes)
    - Premier appel crée l'instance, appels suivants réutilisent le cache
  </behavior>
  <action>
    llm.py — ajouter deux variables module-level `_flash: ChatGoogleGenerativeAI | None = None` et `_pro: ChatGoogleGenerativeAI | None = None`. Dans `flash_llm()` : `global _flash` ; si `_flash is None` → l'instancier (ChatGoogleGenerativeAI(model=GEMINI_MODEL_FLASH, api_key=GEMINI_API_KEY, temperature=0)) ; retourner `_flash`. Idem `pro_llm()` avec `_pro` et GEMINI_MODEL_PRO. Per D-11 (HARD-10).
    NB : ne PAS mémoriser à l'import (la clé/le modèle viennent de config lu une fois). La création paresseuse au 1er appel préserve le fail-fast existant et le monkeypatch des tests (qui remplacent la fonction).
  </action>
  <verify>
    <automated>cd D:/disc_E/vscode_pyhton/python/dataagent && python -m pytest tests/ -q -k "node or critic or graph"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "global _flash" src/dataagent/agent/llm.py` matche
    - `grep -n "global _pro" src/dataagent/agent/llm.py` matche
    - `grep -n "_flash is None" src/dataagent/agent/llm.py` matche la création paresseuse
    - La suite des tests nodes/critic/graph reste verte (monkeypatch transparent)
  </acceptance_criteria>
  <done>flash_llm/pro_llm retournent des singletons ; monkeypatch des tests préservé ; aucune régression</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (aucune nouvelle) | Plan purement perf interne ; pas de nouvelle surface d'entrée |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-10 | (n/a) | singletons llm | accept | Aucune nouvelle surface d'attaque ; la clé API reste chargée depuis l'env, jamais loggée. Risque nul. |
</threat_model>

<verification>
- Tests nodes/critic/graph verts (le cache est transparent pour le monkeypatch)
- flash_llm()/pro_llm() retournent bien des singletons distincts
</verification>

<success_criteria>
- HARD-10 : flash_llm/pro_llm sont des singletons module-level créés au 1er appel
- Aucune régression sur les tests qui monkeypatchent ces fonctions
</success_criteria>

<output>
Après complétion, créer `.planning/phases/07-hardening-bug-fixes/07-03-SUMMARY.md`.
</output>
