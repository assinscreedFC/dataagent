---
phase: 07-hardening-bug-fixes
plan: 04
type: tdd
wave: 3
depends_on: [01, 02, 03]
files_modified:
  - tests/test_cli.py
  - tests/test_nodes.py
  - tests/test_loader.py
  - tests/test_render.py
  - tests/test_api.py
  - tests/test_router_critic_nodes.py
  - tests/test_critic_loop.py
autonomous: true
requirements: [HARD-12]
must_haves:
  truths:
    - "Un test CLI couvre __main__.main : question valide (run mocké) + arg manquant → exit non-zéro"
    - "Un test couvre le chemin échec exécution SQL (EXPLAIN passe / conn.execute raise → retry puis finding erreur)"
    - "Un test couvre les gardes except stats/viz (rows mal formées / arity mismatch)"
    - "Un test couvre le fallback planner vide (LLM whitespace → plan=[question])"
    - "Un test couvre la garde write SQL (DROP rejeté, conn intacte)"
    - "Des tests couvrent la borne current_step (D-01) et l'early-exit critic (D-02) — tests de terminaison existants mis à jour"
    - "Un test couvre le résumé contenu du critic (D-03)"
    - "La couverture globale est ≥85% et la suite complète passe sans régression"
  artifacts:
    - path: "tests/test_cli.py"
      provides: "tests __main__.main (CLI)"
      contains: "main"
  key_links:
    - from: "tests/"
      to: "src/dataagent (toutes les corrections plans 01-03)"
      via: "pytest --cov, vrais I/O DuckDB/FS, LLM mocké"
      pattern: "--cov"
---

<objective>
Combler les blind spots de tests identifiés par la revue et verrouiller les corrections des plans 01-03, en mettant à jour les tests dont le comportement de terminaison change (D-01/D-02). Objectif : ≥85% de couverture globale, zéro régression.

Purpose: HARD-12. Wave 3 — exécuté après que tout le code des plans 01-03 est en place (teste ce code).
Output: nouveaux tests (CLI, exec-failure, except guards, planner-empty, write-guard, current_step bound, critic content, render escape) + mise à jour des tests de terminaison.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-hardening-bug-fixes/07-CONTEXT.md
@.planning/phases/07-hardening-bug-fixes/07-01-SUMMARY.md
@.planning/phases/07-hardening-bug-fixes/07-02-SUMMARY.md
@.planning/phases/07-hardening-bug-fixes/07-03-SUMMARY.md

@src/dataagent/__main__.py
@src/dataagent/agent/nodes.py
@src/dataagent/data/loader.py
@src/dataagent/render.py
@tests/conftest.py
@tests/test_critic_loop.py
@tests/test_router_critic_nodes.py

<interfaces>
<!-- Pattern de test du projet (à respecter strictement) -->
- Vraie I/O DuckDB via fixture `conn` (conftest.py, mini-Olist) ; FS réel (tmp_path).
- LLM mocké UNIQUEMENT : classes _FakeResponse(content) / _FakeLLM(content) / _FakeFlash (call_count séquentiel).
- monkeypatch.setattr(nodes, "flash_llm"/"pro_llm", lambda: _Fake...) pour piloter les réponses.
- __main__.main() : lit sys.argv ; import différé de run ; sys.exit(1) si arg manquant ou clé absente.
- Ne PAS mocker DuckDB/FS.
</interfaces>
</context>

<feature>
  <name>Blind-spot test coverage + mise à jour terminaison (HARD-12)</name>
  <files>tests/test_cli.py (nouveau), tests/test_nodes.py, tests/test_loader.py, tests/test_render.py, tests/test_router_critic_nodes.py, tests/test_critic_loop.py</files>
  <behavior>
    Cas à couvrir (RED puis GREEN — le code existe déjà après plans 01-03, donc les tests doivent passer une fois écrits correctement) :

    CLI (test_cli.py) :
    - main() avec une question valide et `run` monkeypatché (via monkeypatch sur le module importé dans main) → imprime le report, pas de SystemExit (exit 0)
    - main() sans argument positionnel → SystemExit code non-zéro (1) + message usage sur stderr
    - main() sans clé API (monkeypatch env vide) → SystemExit(1)
    - main() avec run() qui raise → SystemExit(1) + message d'erreur sur stderr
    - main() avec --debug → bloc DEBUG imprimé sur stderr

    Échec exécution SQL (test_nodes.py) :
    - _execute_subquestion où le SQL passe EXPLAIN mais conn.execute raise à l'exécution → retry borné puis finding {source:'sql_tool','error':...,'attempts':SQL_MAX_RETRIES+1}, pas de crash (mock flash renvoyant un SQL syntaxiquement valide mais ciblant un état qui fait raise, ou patch conn.execute)

    Gardes except stats/viz (test_nodes.py) :
    - stats_tool_node avec un finding dont rows/columns provoquent l'échec de pl.DataFrame (arity mismatch : columns plus longues que les tuples) → finding insufficient_data, warning loggé (exc_info), pas de crash
    - viz_tool_node idem → finding skipped, pas de crash

    Fallback planner vide (test_nodes.py) :
    - planner_node avec flash renvoyant "   \n  \n" (whitespace) → plan == [question]

    Garde write SQL (test_nodes.py) :
    - _execute_subquestion / sql_tool_node avec flash renvoyant "DROP TABLE orders" → finding d'erreur 'non autorisé', et SELECT count sur orders prouve la table intacte ; pas de retry sur ce motif

    Borne current_step (test_router_critic_nodes.py — MISE À JOUR) :
    - critic_node sur plan=["Q1"] current_step=0 → current_step==0 (borne D-01 : min(1, len-1=0)=0). MAJ l'ancien `test_increments_current_step_by_one` / `test_current_step_increments_from_nonzero` pour refléter la borne (plan de 3, step 2 → 2, pas 3).
    - critic_node sur plan=["Q1","Q2"] current_step=0 → current_step==1

    Early-exit critic & terminaison (test_critic_loop.py — MISE À JOUR) :
    - _critic_decision avec current_step >= len(plan) → "synthesizer" (nouveau test)
    - TestHardCap.test_hard_cap_stops_at_max_iterations : avec D-02, un plan d'1 sous-question termine via l'early-exit (current_step≥len(plan)) AVANT d'atteindre max_iterations. METTRE À JOUR ce test pour qu'il prouve la terminaison correcte : soit fournir un plan multi-étapes qui reboucle réellement, soit asserter que le run termine (report produit) sans exiger iterations==MAX_ITERATIONS. Garder un test dédié qui prouve que le hard cap reste fonctionnel (ex : _critic_decision avec iterations>=max et current_step<len(plan) → 'synthesizer').

    Critic content (test_router_critic_nodes.py ou test_critic_loop.py) :
    - _summarize_findings_for_critic([finding_sql avec rows]) contient la subquestion + un extrait de rows
    - critic_node : le human_msg passé au flash mocké contient le résumé contenu (capturer le prompt via un _FakeLLM qui enregistre les messages)

    Render escape (test_render.py) :
    - render_html avec png_path malicieux → pas de <script> exécutable dans la sortie (valeur échappée)
  </behavior>
  <implementation>
    1. Créer tests/test_cli.py : importer `from dataagent import __main__` ; piloter sys.argv via monkeypatch.setattr(sys, "argv", [...]) ; monkeypatcher la clé API via monkeypatch.setenv/delenv ; monkeypatcher `run` (l'import est différé dans main → patcher `dataagent.agent.graph.run`). Utiliser `pytest.raises(SystemExit)` et vérifier `excinfo.value.code`. Capturer stdout/stderr via capsys.
    2. test_nodes.py : ajouter les cas exec-failure (patch conn.execute pour raise après EXPLAIN ok — ex via monkeypatch sur la méthode, ou SQL valide-au-EXPLAIN qui raise à fetch), gardes except (rows mal formées), planner-empty (whitespace), write-guard (DROP). Réutiliser la fixture `conn`. Mocker flash via monkeypatch.setattr(nodes,"flash_llm",...).
    3. test_router_critic_nodes.py : METTRE À JOUR les assertions current_step pour la borne D-01 (citer "D-01 borne current_step" en commentaire). Ajouter le test de capture du prompt critic (résumé contenu D-03).
    4. test_critic_loop.py : METTRE À JOUR TestHardCap selon D-02 (terminaison anticipée) ; ajouter test _critic_decision early-exit ; conserver un test prouvant le hard cap unitairement.
    5. test_loader.py : ajouter un test nom de table non conforme (CSV nommé pour produire un table_name invalide → skip + warning, autres CSV chargés).
    6. test_render.py : ajouter le test d'échappement png_path.
    7. Lancer la suite complète avec couverture ; combler tout module sous 85% si nécessaire.
    Respecter STRICTEMENT le pattern projet : vrais I/O, LLM mocké uniquement.
  </implementation>
</feature>

<verification>
- `python -m pytest tests/ -q` : suite complète verte, zéro régression (les tests de terminaison mis à jour reflètent D-01/D-02)
- `python -m pytest tests/ --cov=src/dataagent --cov-report=term-missing -q` : couverture globale ≥85%
- `tests/test_cli.py` existe et couvre __main__.main (succès, arg manquant, clé absente, run raise, --debug)
- Nouveaux tests présents : exec-failure SQL, gardes except stats/viz, planner-empty, write-guard, current_step bound, critic content, render escape, loader table-name invalide
</verification>

<success_criteria>
- HARD-12 : blind spots couverts (__main__, échec exec SQL, gardes except, planner vide, write-guard, borne current_step, critic content)
- Couverture globale ≥85%
- Suite complète passe — aucune régression (tests de terminaison mis à jour pour D-01/D-02 uniquement)
</success_criteria>

<output>
Après complétion, créer `.planning/phases/07-hardening-bug-fixes/07-04-SUMMARY.md` (inclure le % de couverture global atteint et la liste des tests existants modifiés pour D-01/D-02).
</output>
