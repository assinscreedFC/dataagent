---
phase: 07-hardening-bug-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/dataagent/agent/state.py
  - src/dataagent/agent/nodes.py
  - src/dataagent/agent/graph.py
autonomous: true
requirements: [HARD-01, HARD-02, HARD-07, HARD-08, HARD-09]
must_haves:
  truths:
    - "current_step ne dépasse jamais len(plan)-1 lors du reboucle critic"
    - "_critic_decision route vers synthesizer dès que toutes les sous-questions sont traitées (avant max_iterations)"
    - "Le critic reçoit un résumé du contenu des findings (rows échantillonnées / analyse / png), pas juste un count"
    - "Le schema DuckDB est introspecté une seule fois par run et propagé via le state"
    - "Tout accès .content LLM passe par _as_text (str garanti, pas de crash multi-part)"
    - "Les except aveugles de stats/viz bindent l'exception et loggent avec exc_info=True"
  artifacts:
    - path: "src/dataagent/agent/state.py"
      provides: "Champ schema:str dans AgentState + initial_state(question, db, schema='')"
      contains: "schema"
    - path: "src/dataagent/agent/nodes.py"
      provides: "_as_text helper, critic_node borné + contenu, _summarize_findings_for_critic, sql_tool_node lit state schema, except bindés"
      contains: "_as_text"
    - path: "src/dataagent/agent/graph.py"
      provides: "_critic_decision early-exit plan exhausted + run() introspecte schema une fois"
      contains: "current_step >= len(plan)"
  key_links:
    - from: "src/dataagent/agent/graph.py:run"
      to: "schema_description(conn)"
      via: "calcul unique injecté dans initial_state"
      pattern: "schema_description"
    - from: "src/dataagent/agent/nodes.py:sql_tool_node"
      to: "state schema"
      via: "state.get('schema') avec fallback schema_description(conn)"
      pattern: "state.get\\(.schema."
    - from: "src/dataagent/agent/nodes.py:critic_node"
      to: "_summarize_findings_for_critic"
      via: "résumé contenu injecté dans le prompt critic"
      pattern: "_summarize_findings_for_critic"
---

<objective>
Corriger les 2 bugs CRITICAL de la boucle agent (current_step overflow + critic aveugle) et durcir le trio state/nodes/graph : type safety sur `.content`, except bindés, et schema introspecté une seule fois par run.

Purpose: HARD-01 (boucle termine correctement, plus de re-run de doublons), HARD-02 (critic juge sur le contenu réel), HARD-07 (mypy clean sur `.content`), HARD-08 (plus de swallow silencieux), HARD-09 (~150 queries/run éliminées). Tout ce qui touche `nodes.py`/`graph.py`/`state.py` est regroupé ici pour éviter des éditions parallèles du même fichier.
Output: state.py (champ schema), nodes.py (helpers + patchs), graph.py (decision + run).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-hardening-bug-fixes/07-CONTEXT.md

@src/dataagent/agent/state.py
@src/dataagent/agent/nodes.py
@src/dataagent/agent/graph.py
@src/dataagent/agent/schema_introspect.py

<interfaces>
<!-- Contrats existants — l'exécuteur les patche, ne les réécrit pas. -->

state.py (actuel) :
  class AgentState(TypedDict): question, plan, findings(add), messages, iterations, max_iterations, current_step, db(UntrackedValue), report
  def initial_state(question: str, db: DuckDBPyConnection) -> AgentState

nodes.py (cibles précises) :
  critic_node(state) -> dict      # ~511-572 ; renvoie {findings, iterations, current_step}
  sql_tool_node(state) -> dict    # ~204-228 ; appelle schema_description(conn) à chaque invoc (à supprimer)
  _execute_subquestion(conn, schema, subquestion) -> dict   # ~131
  planner_node(state) -> dict     # .content ~43
  _generate_sql / _regenerate_sql # .content ~115, ~128
  synthesizer_node(state) -> dict # .content ~600
  stats_tool_node                 # except ~341 (bare)
  viz_tool_node                   # except ~422, ~445 (bare)

graph.py (cibles) :
  _critic_decision(state) -> Literal["router","synthesizer"]  # ~94-121 ; hard cap iterations>=max RESTE
  run(question, conn=None, thread_id=None) -> dict            # ~201 ; appelle initial_state(question, conn) 2× (lignes ~232, ~251)

schema_introspect.py :
  def schema_description(conn: DuckDBPyConnection) -> str   # à appeler UNE fois dans run()
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Schema dans le state (HARD-09) + _as_text helper (HARD-07)</name>
  <files>src/dataagent/agent/state.py, src/dataagent/agent/nodes.py, src/dataagent/agent/graph.py</files>
  <read_first>state.py (entier), nodes.py lignes 1-60 + 204-228 + 575-602, graph.py lignes 201-255, schema_introspect.py</read_first>
  <behavior>
    - _as_text("plain string") == "plain string"
    - _as_text(response) avec response.content multi-part (list de {"text": ...} ou parts) retourne la concaténation texte, jamais un crash
    - _as_text(response) avec response.content non-str non-list retourne str(content)
    - initial_state("q", conn) garde schema="" par défaut (rétrocompat signature à 2 args)
    - initial_state("q", conn, schema="TABLE x(...)")["schema"] == "TABLE x(...)"
    - sql_tool_node sur un state avec schema="" tombe en fallback schema_description(conn) (tests node-level isolés inchangés)
    - sql_tool_node sur un state avec schema fourni n'appelle PAS schema_description (utilise la valeur du state)
  </behavior>
  <action>
    1. state.py — ajouter le champ `schema: str` dans `AgentState` (str → msgpack-sérialisable, ne casse pas _FilteredSqliteSaver). Modifier `initial_state(question, db, schema: str = "")` et inclure `schema=schema` dans le AgentState retourné (défaut "" préserve l'appel à 2 args).
    2. nodes.py — créer le helper `_as_text(response) -> str` (D-08) : si `response.content` est `str` → le retourner ; si c'est une liste → concaténer les parts texte (`p.get("text", "")` pour les dict, `str(p)` sinon) ; sinon `str(response.content)`. Placer le helper en haut du module après les imports.
    3. nodes.py — remplacer CHAQUE `.content` brut par `_as_text(response)` : planner_node (~43), _generate_sql (~115), _regenerate_sql (~128), critic_node (~549, traité en Task 2 mais appliquer ici si touché), synthesizer_node (~600). Corrige les 5 erreurs mypy.
    4. nodes.py — `sql_tool_node` (~213-214) : remplacer `schema = schema_description(conn)` par `schema = state.get("schema") or schema_description(conn)` (fallback préserve les tests node-level qui ne fournissent pas de schema). NE PAS retirer l'import schema_description (utilisé en fallback).
    5. graph.py — `run()` : après avoir obtenu `conn` (les deux branches thread_id), calculer `schema = schema_description(conn)` UNE fois, et passer `initial_state(question, conn, schema=schema)` aux deux endroits (~232 et ~251). Importer `schema_description` depuis `dataagent.agent.schema_introspect`.
    Citer "per D-08 (HARD-07)" et "per D-10 (HARD-09)" dans les docstrings/commentaires modifiés.
  </action>
  <verify>
    <automated>cd D:/disc_E/vscode_pyhton/python/dataagent && python -m pytest tests/test_state.py tests/test_nodes.py tests/test_graph.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "_as_text" src/dataagent/agent/nodes.py` retourne la définition + ≥4 usages (planner, gen, regen, synth)
    - `grep -n "schema: str" src/dataagent/agent/state.py` matche le champ
    - `grep -n 'state.get("schema")' src/dataagent/agent/nodes.py` matche le fallback dans sql_tool_node
    - `grep -n "schema_description" src/dataagent/agent/graph.py` matche l'appel unique dans run()
    - Pas de `\.content` brut résiduel dans nodes.py : `grep -n "\.content" src/dataagent/agent/nodes.py` ne doit montrer QUE des occurrences à l'intérieur de `_as_text` lui-même
  </acceptance_criteria>
  <done>schema:str dans le state, introspecté 1×/run, propagé à sql_tool avec fallback ; _as_text utilisé après chaque .content ; tests state/nodes/graph verts</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: critic_node borné + contenu (HARD-01 partiel, HARD-02) + _critic_decision early-exit (HARD-01)</name>
  <files>src/dataagent/agent/nodes.py, src/dataagent/agent/graph.py</files>
  <read_first>nodes.py lignes 511-572 (critic_node), graph.py lignes 94-121 (_critic_decision), tests/test_router_critic_nodes.py (TestCriticNode), tests/test_critic_loop.py (TestHardCap)</read_first>
  <behavior>
    - critic_node sur plan=["Q1"] current_step=0 → current_step=1 (min(0+1, len-1)=0 NON : voir règle D-01) — règle exacte : next_step = min(current_step+1, len(plan)-1) si plan non vide, sinon 0. Donc plan=["Q1"], current_step=0 → next=min(1,0)=0
    - critic_node sur plan=["Q1","Q2"] current_step=0 → next=min(1,1)=1
    - critic_node sur plan=["Q1","Q2","Q3"] current_step=2 → next=min(3,2)=2
    - critic_node sur plan=[] current_step=5 → next=0
    - _summarize_findings_for_critic([finding_sql]) contient la subquestion + un extrait de rows[:2]
    - _summarize_findings_for_critic retourne une chaîne ≤ ~1500 chars (cap)
    - critic prompt humain contient le résumé contenu, plus "N finding(s) — sources: ..."
    - _critic_decision avec current_step >= len(plan) → "synthesizer" (avant le check sufficient), MAIS le hard cap iterations>=max reste prioritaire/présent
  </behavior>
  <action>
    1. nodes.py — `critic_node` : remplacer `current_step + 1` (ligne ~571) par la borne D-01 :
       `plan = state.get("plan") or []`
       `next_step = min(current_step + 1, len(plan) - 1) if plan else 0`
       et retourner `"current_step": next_step`. Plus de dépassement de plan[].
    2. nodes.py — créer `_summarize_findings_for_critic(findings: list[dict]) -> str` (D-03) : pour chaque finding non-critic, une ligne courte selon source : sql_tool → `subquestion` + `rows[:2]` (ou son `error`), stats_tool → `analysis` + columns/value/anomalies, viz_tool → `png_path généré` ou `skipped`. Joindre par `\n`, tronquer à 1500 chars (suffixe `"…"` si coupé). Si findings vide → "(aucun finding)".
    3. nodes.py — `critic_node` : remplacer le `findings_summary` actuel (count only, ~531-534) par l'injection du résumé contenu : garder une 1re ligne `"{len} finding(s) — sources: ..."` PUIS ajouter le bloc `_summarize_findings_for_critic(findings)` dans le `human_msg`.
    4. nodes.py — utiliser `_as_text(response)` pour `raw` dans critic_node (~549) si pas déjà fait en Task 1.
    5. graph.py — `_critic_decision` (~105) : AVANT le check `sufficient` et APRÈS (ou avec) le hard cap, ajouter l'early-exit D-02 :
       `current_step = state.get("current_step", 0)` ; `plan = state.get("plan", [])` ;
       `if current_step >= len(plan): return "synthesizer"` — placé juste après le hard cap `if iterations >= max_iterations: return "synthesizer"` (le hard cap RESTE en place, on AJOUTE l'early-exit). Documenter "per D-02 (HARD-01) — termine dès que toutes les sous-questions sont traitées, en plus du hard cap existant".
  </action>
  <verify>
    <automated>cd D:/disc_E/vscode_pyhton/python/dataagent && python -m pytest tests/test_router_critic_nodes.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "min(current_step + 1" src/dataagent/agent/nodes.py` matche la borne D-01
    - `grep -n "_summarize_findings_for_critic" src/dataagent/agent/nodes.py` retourne la définition + l'usage dans critic_node
    - `grep -n "current_step >= len(plan)" src/dataagent/agent/graph.py` matche l'early-exit D-02
    - Le hard cap reste : `grep -n "iterations >= max_iterations" src/dataagent/agent/graph.py` matche toujours
    - TestCriticNode existant passe (l'assertion `test_increments_current_step_by_one` sur plan=["Q1","Q2"] → current_step==1 reste vraie ; `test_current_step_increments_from_nonzero` plan de 3, step 2 → 2 : NB cet ancien test attend 3 — il sera mis à jour dans le plan 04/D-13 car D-01 change la borne). Si ce test échoue, NE PAS modifier le code : le laisser pour Task tests (D-13). Documenter le delta dans le SUMMARY.
  </acceptance_criteria>
  <done>current_step borné par D-01, critic juge sur le contenu (D-03), _critic_decision sort tôt quand le plan est épuisé (D-02), hard cap préservé</done>
</task>

<task type="auto">
  <name>Task 3: except bindés + exc_info (HARD-08)</name>
  <files>src/dataagent/agent/nodes.py</files>
  <read_first>nodes.py lignes 338-343 (stats DataFrame), 419-424 (viz DataFrame), 434-447 (render_chart)</read_first>
  <action>
    Dans `stats_tool_node` (~341), `viz_tool_node` (~422 reconstruction DataFrame, ~445 render_chart) : remplacer chaque `except Exception:  # noqa: BLE001` aveugle par `except Exception as exc:  # noqa: BLE001` et ajouter `exc_info=True` au `logger.warning(...)` existant (passer aussi `exc` dans le message si utile). Conserver STRICTEMENT le comportement (continue/skip/finding) — on ajoute uniquement la traçabilité. Per D-09 (HARD-08).
    NB : `_validate_sql` (~83) et `_execute_subquestion` (~176) loggent déjà l'erreur — ne pas régresser, mais s'aligner sur exc_info=True si ce n'est pas déjà fait.
  </action>
  <verify>
    <automated>cd D:/disc_E/vscode_pyhton/python/dataagent && python -m pytest tests/test_nodes.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "except Exception as exc" src/dataagent/agent/nodes.py` matche ≥3 fois (stats, viz DataFrame, viz render)
    - `grep -n "exc_info=True" src/dataagent/agent/nodes.py` matche les warnings des gardes stats/viz
    - Aucun `except Exception:` aveugle résiduel sans `as exc` dans stats_tool_node/viz_tool_node
  </acceptance_criteria>
  <done>Les gardes except stats/viz bindent l'exception et loggent exc_info=True, comportement nominal inchangé</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| question → planner → SQL gen | L'input utilisateur influence le SQL généré par le LLM (prompt injection possible) |
| LLM response → .content | Réponse LLM non fiable (multi-part, type inattendu) consommée par le code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | critic_node boucle | mitigate | current_step borné (D-01) + _critic_decision early-exit (D-02) — empêche la boucle de re-traiter/dépasser plan[] et de gaspiller des itérations |
| T-07-02 | Denial of Service | _as_text / .content | mitigate | _as_text neutralise les réponses LLM multi-part qui crashaient le run (D-08) |
| T-07-03 | Information Disclosure | except aveugles | mitigate | binding + exc_info=True trace les erreurs au lieu de les avaler (D-09) ; aucun secret loggé |
| T-07-04 | Tampering (SQL write/DDL) | _execute_subquestion | transfer | Traité dans le plan 07-02 (write-guard D-04) — hors scope de ce plan |
</threat_model>

<verification>
- `python -m pytest tests/test_state.py tests/test_nodes.py tests/test_graph.py tests/test_router_critic_nodes.py -q` passe (sauf les 1-2 tests dont le comportement de terminaison change par D-01/D-02 — ils seront mis à jour dans le plan 07-04/D-13 ; documenter le delta exact dans le SUMMARY)
- Aucun `.content` brut hors `_as_text` dans nodes.py
- schema_description appelé une seule fois dans run() (pas dans sql_tool_node sauf fallback)
</verification>

<success_criteria>
- HARD-01 : current_step ≤ len(plan)-1 ; _critic_decision sort vers synthesizer quand le plan est épuisé (hard cap préservé)
- HARD-02 : le critic reçoit le contenu des findings via _summarize_findings_for_critic
- HARD-07 : _as_text utilisé après chaque .content (mypy clean)
- HARD-08 : except stats/viz bindés + exc_info=True
- HARD-09 : schema introspecté 1×/run, propagé via state.schema avec fallback
- Comportement nominal préservé (run normal produit le même type de rapport)
</success_criteria>

<output>
Après complétion, créer `.planning/phases/07-hardening-bug-fixes/07-01-SUMMARY.md` (inclure explicitement le DELTA de comportement de terminaison introduit par D-01/D-02 et la liste des tests existants à mettre à jour dans le plan 07-04).
</output>
